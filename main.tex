\documentclass[12pt, a4paper]{report}

% Packages indispensables
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{listings}
\usepackage{mdframed}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.multipart, arrows.meta, positioning,
  calc, fit, backgrounds, chains, decorations.pathreplacing}
% Rend TikZ compatible avec les caractères actifs du babel français (; : ! ?)
\usetikzlibrary{babel}

% ------------------------------------------------------------------
% Styles TikZ réutilisables pour les diagrammes du mémoire
% ------------------------------------------------------------------
\tikzset{
  % --- Diagramme de contexte / composants ---
  syst/.style={rectangle, draw=black, thick, rounded corners=5pt, fill=blue!15,
    align=center, minimum height=14mm, minimum width=32mm, font=\bfseries\small},
  box/.style={rectangle, draw=black, rounded corners=3pt, fill=blue!8,
    align=center, minimum height=9mm, inner sep=6pt, font=\small},
  extbox/.style={rectangle, draw=black, fill=gray!15, align=center,
    minimum height=10mm, inner sep=6pt, font=\small},
  comp/.style={rectangle, draw=black, fill=blue!8, align=center,
    minimum height=12mm, minimum width=34mm, inner sep=6pt, font=\small},
  layer/.style={rectangle, draw=black, rounded corners=3pt, align=center,
    minimum height=12mm, minimum width=46mm, inner sep=6pt, font=\small},
  flow/.style={-{Stealth[length=2.6mm]}, thick},
  flowlab/.style={font=\scriptsize, fill=white, inner sep=1.5pt},
  dep/.style={-{Stealth[length=2.6mm]}, thick, dashed},
  % --- Cas d'utilisation ---
  usecase/.style={ellipse, draw=black, fill=yellow!18, align=center,
    minimum width=30mm, minimum height=11mm, font=\scriptsize, inner sep=1pt},
  ucsystem/.style={rectangle, draw=black, thick, rounded corners=2pt},
  % --- Diagramme d'activités ---
  act/.style={rectangle, rounded corners=4pt, draw=black, fill=blue!8,
    align=center, minimum height=8mm, minimum width=30mm, font=\scriptsize, inner sep=4pt},
  decision/.style={diamond, draw=black, fill=orange!20, aspect=2.2,
    align=center, font=\scriptsize, inner sep=0pt},
  init/.style={circle, draw=black, fill=black, minimum size=4.5mm, inner sep=0pt},
  final/.style={circle, draw=black, minimum size=6.5mm, inner sep=0pt,
    path picture={\fill (path picture bounding box.center) circle (1.6mm);}},
  % --- Classes / objets UML ---
  umlclass/.style={rectangle, draw=black, fill=white, font=\scriptsize,
    rectangle split, rectangle split parts=3, align=left, inner sep=4pt, text width=36mm},
  umlobject/.style={rectangle, draw=black, fill=blue!6, font=\scriptsize,
    rectangle split, rectangle split parts=2, align=left, inner sep=4pt, text width=40mm},
  % --- MLD : tables relationnelles ---
  table/.style={rectangle, draw=black, fill=white, font=\scriptsize,
    rectangle split, rectangle split parts=2, align=left, inner sep=4pt, text width=42mm},
  % --- Entités MCD (Merise) ---
  entite/.style={rectangle, draw=black, thick, fill=blue!10, font=\scriptsize,
    rectangle split, rectangle split parts=2, align=left, inner sep=4pt, text width=34mm},
  assoc/.style={ellipse, draw=black, fill=green!12, align=center,
    font=\scriptsize, minimum width=22mm, minimum height=11mm, inner sep=1pt},
  card/.style={font=\scriptsize, fill=white, inner sep=1pt},
  % --- Diagramme de séquence ---
  actorbox/.style={rectangle, draw=black, fill=blue!12, font=\scriptsize\bfseries,
    minimum height=8mm, align=center, inner sep=4pt},
  lifeline/.style={dashed, gray!70},
  activation/.style={rectangle, draw=black, fill=blue!15, minimum width=2.2mm},
  msg/.style={-{Stealth[length=2mm]}, font=\scriptsize},
  retmsg/.style={-{Stealth[length=2mm]}, dashed, font=\scriptsize},
}

% Acteur UML (bonhomme bâton) réutilisable
\newcommand{\umlactor}[1]{%
  \begin{tikzpicture}[baseline=-2mm, scale=0.5]
    \draw[thick] (0,0.7) circle (0.18);
    \draw[thick] (0,0.52) -- (0,-0.1);
    \draw[thick] (-0.3,0.32) -- (0.3,0.32);
    \draw[thick] (0,-0.1) -- (-0.25,-0.55);
    \draw[thick] (0,-0.1) -- (0.25,-0.55);
  \end{tikzpicture}\\[2pt]\footnotesize\textbf{#1}}

% Configuration de la mise en page
\geometry{top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm}

% Configuration des liens
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=magenta,      
    urlcolor=blue,
    pdftitle={Mémoire CARIBOOKS - Alexandre REY},
}

% Style SQL listings
\lstdefinestyle{sqlstyle}{
    language=SQL,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{gray}\itshape,
    stringstyle=\color{orange},
    showstringspaces=false,
    breaklines=true,
    frame=single,
    rulecolor=\color{black!30},
    backgroundcolor=\color{black!5},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=5pt,
    tabsize=2,
}

% Style Python listings
\lstdefinestyle{pythonstyle}{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{gray}\itshape,
    stringstyle=\color{orange},
    showstringspaces=false,
    breaklines=true,
    frame=single,
    rulecolor=\color{black!30},
    backgroundcolor=\color{black!5},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=5pt,
    tabsize=4,
}

% Boîte "À compléter"
\newmdenv[
  linecolor=red!70,
  linewidth=2pt,
  backgroundcolor=red!5,
  skipabove=12pt,
  skipbelow=12pt,
  innertopmargin=10pt,
  innerbottommargin=10pt,
]{acompleter}

% Style des en-têtes et pieds de page
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Projet CARIBOOKS}
\fancyhead[R]{Alexandre REY - ESTIAM}
\fancyfoot[C]{\thepage}

\begin{document}

% ==========================================
% 1. PAGE DE COUVERTURE
% ==========================================
\begin{titlepage}
    \centering
    \vspace*{2cm}
    
    {\Large \textbf{École ESTIAM} }\\[2cm]
    
    {\huge \textbf{MÉMOIRE DE PROJET}}\\[1cm]
    {\Huge \textbf{CARIBOOKS}}\\[2cm]
  
    \begin{flushleft}
        \large
        \textbf{Présenté par :} Alexandre REY \\
        \textbf{Classe et spécialité :} E3 DAD (Data and Application Development) \\[0.5cm]
        \textbf{Encadrant :} Mhand Boufala \\
        \textbf{Année scolaire :} 2025-2026 \\
    \end{flushleft}
    
    \vfill
    \vspace{2cm}
\end{titlepage}

% ==========================================
% 2. ABSTRACT (Résumé)
% ==========================================
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

\textbf{Contexte du projet :} \\
Ce projet s'inscrit dans le cadre du titre CDA (Concepteur Développeur d'Applications) et fait suite à une demande réelle d'une bénévole de la recyclerie de Caritas, une association facilitant la réinsertion professionnelle par la vente d'objets de seconde main. Face à un stock de livres invendus, la demande était de créer une plateforme de vente en ligne dédiée, adaptée aux contraintes locales suisses.

Les plateformes de revente existantes fonctionnant principalement en euros et sans restriction géographique, elles ne correspondaient pas au besoin. La plateforme CARIBOOKS a donc été conçue pour opérer exclusivement en Francs Suisses (CHF), avec une livraison restreinte au territoire suisse et un système d'ajout rapide de livres via scan du code ISBN.

\vspace{20pt}

\textbf{Objectif et résultats attendus :} \\
L'objectif principal est de concevoir et déployer une plateforme e-commerce solidaire (nommée CARIBOOKS), facile d'utilisation pour les bénévoles de Caritas. Elle doit offrir une gestion simplifiée des ventes — de l'ajout rapide de produits grâce à l'API ISBN jusqu'au suivi des commandes — le tout en respectant les contraintes locales (devise CHF, livraison suisse, et paiement via PostFinance, prestataire suisse intégré au service de livraison de la Poste Suisse).

\newpage

% ==========================================
% 3. SOMMAIRE
% ==========================================
\tableofcontents
\newpage

% ==========================================
% 4. LISTE DES FIGURES
% ==========================================
\listoffigures
\newpage

% ==========================================
% 5. INTRODUCTION GÉNÉRALE
% ==========================================
\chapter*{Introduction générale}
\addcontentsline{toc}{chapter}{Introduction générale}

\textit{[À rédiger une fois le mémoire totalement terminé.]}

\noindent Dans le cadre du titre CDA (Concepteur Développeur d'Applications), ce projet représente une opportunité de répondre à une demande utilisateur réelle : créer une plateforme e-commerce solidaire pour la recyclerie de l'association Caritas. Ce projet est né d'un constat simple — de nombreux livres d'occasion finissent invendus, alors qu'une mise en ligne adaptée au marché suisse pourrait leur donner une seconde vie tout en soutenant la réinsertion professionnelle.

\noindent La problématique centrale est la suivante : \textit{comment concevoir et déployer une application e-commerce sur mesure, contrainte au territoire suisse (devise CHF), ergonomique pour des bénévoles non technophiles, et permettant un ajout de stock rapide via scan ISBN ?}

\noindent Pour mener ce projet, une méthodologie Agile a été adoptée, avec des points réguliers avec la demandeuse (qui joue le rôle de Product Owner informelle) pour valider les fonctionnalités développées et ajuster les priorités. Ce mémoire est structuré en quatre chapitres : le contexte et l'état de l'art, l'analyse et la conception, la réalisation et la mise en \oe{}uvre, et enfin les tests et la validation.

% ==========================================
% 6. LES CHAPITRES PRINCIPAUX
% ==========================================

% ==========================================
% CHAPITRE A — PRÉSENTATION DES COMPÉTENCES
% ==========================================
\chapter{Présentation des compétences}

\section{Blocs de compétences du référentiel CDA adressés}

Ce projet couvre l'intégralité des trois blocs de compétences du référentiel CDA (RNCP) tel que défini par France Compétences.

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{@{}p{1cm}p{5cm}p{8cm}@{}}
\toprule
\textbf{Bloc} & \textbf{Intitulé} & \textbf{Compétences mobilisées dans CARIBOOKS} \\
\midrule
1 & Développer une application sécurisée &
  Authentification JWT avec \texttt{python-jose} et hashage \texttt{bcrypt}, validation des données avec Pydantic, contrôle des permissions par rôle (admin / user), requêtes SQL préparées via SQLAlchemy (ORM), gestion des sessions sécurisées, protection des routes FastAPI avec middleware d'autorisation. \\
\midrule
2 & Concevoir et développer une application multicouche &
  Modélisation de la base de données (MCD/MLD MySQL), architecture REST API (FastAPI), frontend découplé (Next.js / React / TypeScript), séparation stricte des couches métier, données et présentation, intégration d'APIs tierces (OpenLibrary, PostFinance), conteneurisation Docker avec \texttt{docker-compose}. \\
\midrule
3 & Préparer le déploiement d'une application &
  Conteneurisation complète de l'application (API + frontend + BDD), pipeline CI/CD via GitHub Actions, déploiement sur environnement cloud (frontend sur Vercel, backend et base MySQL sur Oracle Cloud), gestion des variables d'environnement, documentation API Swagger générée automatiquement par FastAPI. \\
\bottomrule
\end{tabular}
\caption{Couverture des trois blocs de compétences CDA}
\label{tab:blocs_cda}
\end{table}

\section{Liste détaillée des compétences mises en \oe{}uvre}

\subsection{Compétences techniques}
\begin{itemize}
    \item Développement backend avec \textbf{FastAPI} (Python) : création d'endpoints REST, gestion des dépendances, validation automatique des données via Pydantic.
    \item Développement frontend avec \textbf{Next.js} (React / TypeScript) : rendu côté serveur (SSR), routing Next.js, composants réutilisables.
    \item Modélisation et gestion d'une base de données \textbf{MySQL} : conception MCD/MLD, rédaction du DDL SQL, utilisation de l'ORM SQLAlchemy.
    \item \textbf{Authentification JWT} : génération et validation de tokens, gestion des rôles utilisateurs, hashage des mots de passe avec bcrypt.
    \item \textbf{Scan ISBN} via librairie JavaScript de lecture de codes-barres dans le navigateur, avec appel à l'API OpenLibrary pour le préremplissage des métadonnées.
    \item Intégration de l'\textbf{API PostFinance} pour le paiement en CHF (TWINT, Visa, Mastercard), compatible avec le service de livraison de la Poste Suisse.
    \item \textbf{Conteneurisation} avec Docker et docker-compose : orchestration multi-services (API, frontend, MySQL).
    \item Mise en place d'un pipeline \textbf{CI/CD} avec GitHub Actions.
    \item Rédaction de \textbf{tests unitaires et d'intégration} avec pytest.
\end{itemize}

\subsection{Compétences transversales}
\begin{itemize}
    \item Gestion de projet en méthodologie \textbf{Agile (Scrum)} : planification en sprints, backlog, suivi via Microsoft Planner.
    \item Communication avec une \textbf{Product Owner non technique} : reformulation des besoins, conduite de démos, recueil des retours utilisateurs.
    \item \textbf{Veille technologique} et sécurité (OWASP, bonnes pratiques FastAPI).
    \item Gestion du code source avec \textbf{Git} et \textbf{GitHub} (licence GitHub for Students Pro).
\end{itemize}

% --- CHAPITRE 1 ---
\chapter{Contexte et état de l'art}

\section{Présentation du domaine du projet}

Le projet s'inscrit dans le domaine du commerce en ligne (e-commerce) à visée solidaire. Avec l'évolution des habitudes de consommation, la vente d'articles de seconde main connaît un essor majeur : en 2024, le marché mondial de la seconde main dépasse les 128 milliards d'euros, avec une croissance annuelle à deux chiffres portée par des considérations économiques et écologiques. En Suisse, ce phénomène est particulièrement marqué : 57\% de la population pratique régulièrement l'achat-revente en ligne, plaçant la Suisse en tête des pays européens sur ce segment.

\noindent Concernant spécifiquement le livre d'occasion, la part de marché progresse chaque année pour atteindre près de 20\% des livres achetés en 2022, avec une augmentation constante du nombre d'acheteurs. Le marché est désormais massivement opéré en ligne, par des acteurs hétérogènes allant des plateformes de mise en relation entre particuliers aux détaillants entièrement numériques. Dans ce contexte, la recyclerie de Caritas, association facilitant la réinsertion professionnelle en Suisse romande, cherche à digitaliser la vente de ses livres d'occasion pour éviter les invendus et maximiser son impact social.

\section{Étude des solutions existantes}

Il existe plusieurs plateformes de revente de livres et d'objets d'occasion sur le marché. Les principales sont :

\begin{itemize}
    \item \textbf{Momox} : plateforme spécialisée dans le rachat et la revente de livres, CD et jeux vidéo. Opère principalement en Europe avec tarification en euros.
    \item \textbf{Vinted} : place de marché généraliste entre particuliers, très populaire en Europe. Interface simple mais sans gestion spécifique des livres ni support natif du CHF.
    \item \textbf{Shopify} : solution SaaS e-commerce généraliste permettant de créer une boutique en ligne. Flexible mais coûteux à l'usage (abonnement mensuel + frais de transaction) et nécessitant des compétences techniques pour la personnalisation.
    \item \textbf{WooCommerce} : plugin e-commerce pour WordPress. Open-source et modulable, mais le back-office peut être complexe pour des utilisateurs non technophiles.
    \item \textbf{Ricardo.ch} : principale plateforme de vente entre particuliers en Suisse, opérant en CHF. Cependant, elle ne propose pas de back-office dédié pour les associations ni d'intégration ISBN.
\end{itemize}

\section{Comparaison des solutions et Analyse critique}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{@{}p{2.8cm}p{1.8cm}p{2cm}p{2cm}p{2cm}p{2.5cm}@{}}
\toprule
\textbf{Solution} & \textbf{Devise CHF} & \textbf{Scan ISBN} & \textbf{Back-office simplifié} & \textbf{Coût} & \textbf{Personnalisable} \\
\midrule
Momox & \textcolor{red}{Non} & \textcolor{green}{Oui} & \textcolor{red}{Non} & Gratuit & \textcolor{red}{Non} \\
Vinted & \textcolor{red}{Non} & \textcolor{red}{Non} & \textcolor{green}{Partiel} & Gratuit & \textcolor{red}{Non} \\
Shopify & \textcolor{green}{Oui} & \textcolor{red}{Non} & \textcolor{green}{Oui} & Élevé & \textcolor{green}{Partiel} \\
WooCommerce & \textcolor{green}{Oui} & \textcolor{red}{Non} & \textcolor{red}{Non} & Moyen & \textcolor{green}{Oui} \\
Ricardo.ch & \textcolor{green}{Oui} & \textcolor{red}{Non} & \textcolor{red}{Non} & Moyen & \textcolor{red}{Non} \\
\textbf{CARIBOOKS} & \textcolor{green}{\textbf{Oui}} & \textcolor{green}{\textbf{Oui}} & \textcolor{green}{\textbf{Oui}} & \textbf{Faible} & \textcolor{green}{\textbf{Total}} \\
\bottomrule
\end{tabular}
\caption{Comparaison des solutions existantes avec CARIBOOKS}
\label{tab:comparaison}
\end{table}

\noindent Bien que les solutions existantes soient performantes dans leur domaine, elles présentent des limites critiques pour le besoin spécifique de Caritas :

\begin{itemize}
    \item \textbf{Devise et géolocalisation :} La plupart des plateformes standardisées imposent l'euro ou manquent de flexibilité pour forcer exclusivement l'utilisation du CHF et restreindre les expéditions à la Suisse.
    \item \textbf{Ergonomie pour les bénévoles :} Les back-offices des CMS classiques (Shopify, WooCommerce) sont souvent complexes pour des utilisateurs non technophiles.
    \item \textbf{Ajout de stock :} Peu d'outils intègrent nativement un scan de code-barres ISBN pour automatiser la récupération des métadonnées (titre, auteur, résumé, couverture) de façon fluide.
    \item \textbf{Coût de maintenance :} Les solutions SaaS comme Shopify engendrent des frais mensuels récurrents incompatibles avec les contraintes budgétaires d'une association caritative.
\end{itemize}

\section{Choix de la solution}

Face à ces limites, le choix s'est porté sur un développement sur-mesure de l'application CARIBOOKS. La demandeuse souhaitait expressément une solution modifiable, adaptée à leurs besoins spécifiques, avec un coût de maintenance minimal. Ce positionnement permet :

\begin{itemize}
    \item L'intégration d'un module de scan ISBN conçu spécifiquement pour la saisie rapide des ouvrages par les bénévoles.
    \item Le contrôle total sur le tunnel d'achat (devise CHF uniquement, expéditions limitées à la Suisse).
    \item La création d'une interface d'administration (back-office) épurée, allant à l'essentiel.
    \item Un coût de maintenance faible grâce à une architecture open-source (FastAPI + Next.js) sans abonnement tiers obligatoire, et un paiement géré par \textbf{PostFinance}, prestataire suisse intégré à l'écosystème de la Poste Suisse, permettant notamment une gestion native des étiquettes de livraison PostPac.
\end{itemize}

\section{Justification du choix de PostFinance}

Le choix de PostFinance comme prestataire de paiement s'est imposé pour plusieurs raisons pratiques et techniques :

\begin{itemize}
    \item \textbf{Environnement de test accessible :} PostFinance met à disposition un environnement sandbox complet ne nécessitant pas d'inscription avec une carte bancaire réelle avant la mise en production, ce qui facilite grandement le développement et les tests.
    \item \textbf{Intégration native avec la Poste Suisse :} La synergie entre le paiement PostFinance et le service de livraison PostPac/Swiss Post permet d'envisager une gestion unifiée des commandes et des expéditions, particulièrement pertinente pour une plateforme à destination exclusive du marché suisse.
    \item \textbf{Support des moyens de paiement suisses :} PostFinance supporte nativement TWINT (paiement mobile dominant en Suisse), PostCard, Visa et Mastercard, ce qui couvre l'ensemble des préférences de paiement du public cible.
    \item \textbf{Conformité locale :} Acteur 100\% suisse, PostFinance garantit une conformité avec les réglementations financières helvétiques et inspire confiance auprès des utilisateurs locaux.
\end{itemize}

% --- CHAPITRE 2 ---
\chapter{Analyse et conception}

\section{Analyse des besoins}

\subsection{Problématique et public cible}

L'association Caritas dispose d'un stock de livres d'occasion qu'elle ne parvient pas à écouler entièrement en magasin. L'enjeu est de permettre une mise en vente en ligne simple et rapide, sans contraindre les bénévoles à saisir manuellement toutes les informations de chaque ouvrage. La plateforme CARIBOOKS cible trois profils d'utilisateurs distincts :

\begin{itemize}
    \item \textbf{Les associations caritatives} (notamment Caritas) qui souhaitent éviter de jeter leurs livres invendus en les proposant à la vente en ligne.
    \item \textbf{Les étudiants} à la recherche de livres à prix abordable.
    \item \textbf{Les lecteurs en Suisse} souhaitant acheter des livres d'occasion avec des prix affichés exclusivement en CHF.
\end{itemize}

\subsection{Besoins fonctionnels}

Les besoins fonctionnels sont organisés en trois objectifs mesurables :

\vspace{6pt}
\noindent\textbf{Objectif 1 — Dépôt d'annonce via scan ISBN :}\\
Permettre à un vendeur d'ajouter un livre à vendre en scannant son ISBN. Lorsque le code est reconnu, les données principales (titre, auteur, éditeur, couverture) sont récupérées automatiquement via l'API OpenLibrary. Le vendeur complète ensuite l'état du livre et le prix en CHF, puis publie son annonce.

\vspace{6pt}
\noindent\textbf{Objectif 2 — Recherche et filtres pour l'acheteur :}\\
L'acheteur peut parcourir le catalogue et filtrer les livres disponibles selon les critères suivants :
\begin{itemize}
    \item Titre
    \item Auteur
    \item Catégorie / genre
    \item État du livre (\textit{comme neuf}, \textit{bon état}, \textit{état correct})
    \item Prix en CHF
\end{itemize}

\vspace{6pt}
\noindent\textbf{Objectif 3 — Gestion des annonces et espace utilisateur :}\\
L'association dispose d'un espace de gestion de ses annonces (création, modification, suppression). Les acheteurs peuvent gérer leur liste de favoris et, dans une version future, laisser des avis sur les livres et contacter le vendeur pour négocier.

\subsection{Fonctionnalités du MVP (Phase 1)}

\begin{itemize}
    \item Inscription et connexion des utilisateurs
    \item Ajout d'un livre par scan ISBN ou saisie manuelle
    \item Préremplissage automatique des métadonnées via API OpenLibrary
    \item Saisie du prix en CHF
    \item Sélection de l'état : \textit{comme neuf}, \textit{bon état}, \textit{état correct}
    \item Publication et suppression d'annonces
    \item Catalogue de livres disponibles
    \item Moteur de recherche avec filtres (titre, auteur, genre, prix, état)
    \item Fiche détaillée du livre avec informations essentielles et coordonnées du vendeur
    \item Paiement sécurisé via PostFinance (TWINT, Visa/Mastercard en CHF)
\end{itemize}

\subsection{Critères de succès}

Du point de vue utilisateur, le MVP est considéré comme réussi si l'application permet de :
\begin{itemize}
    \item Mettre un livre en vente en \textbf{moins de 2 minutes}
    \item Trouver rapidement un livre d'occasion avec des critères simples
    \item Visualiser clairement le prix en CHF, l'état du livre et les informations principales
    \item Procéder à un paiement en CHF sans friction via TWINT ou carte bancaire
\end{itemize}

\subsection{Acteurs et responsabilités}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{@{}p{3.2cm}p{4.5cm}p{6.5cm}@{}}
\toprule
\textbf{Acteur} & \textbf{Objectif principal} & \textbf{Actions autorisées} \\
\midrule
Administrateur du site & Assurer le bon fonctionnement et la qualité des annonces & CRUD sur les annonces, modération, gestion des comptes utilisateurs \\
Vendeur (bénévole Caritas) & Mettre en vente rapidement les livres du stock & Ajouter un livre (scan ISBN ou saisie manuelle), fixer un prix en CHF, modifier ou supprimer ses annonces \\
Acheteur & Trouver et acheter un livre d'occasion à bon prix & Rechercher, filtrer, consulter les fiches, ajouter en favoris, acheter via PostFinance \\
Modérateur & Garantir la qualité et la conformité des contenus & Vérifier, masquer ou supprimer les annonces non conformes \\
\bottomrule
\end{tabular}
\caption{Acteurs du système CARIBOOKS et leurs responsabilités}
\label{tab:acteurs}
\end{table}

\subsection{Besoins non fonctionnels et contraintes}

\begin{itemize}
    \item \textbf{Délai :} Environ 3 mois, développement réalisé en solo sur la partie technique, le soir (17h–20h, 3 jours par semaine).
    \item \textbf{Sécurité :} Authentification sécurisée (JWT), validation côté serveur via Pydantic, contrôle strict des permissions par rôle.
    \item \textbf{Scalabilité :} Architecture conteneurisée (Docker) permettant la montée en charge si le nombre d'utilisateurs augmente.
    \item \textbf{Dépendance API :} La fonctionnalité de scan ISBN repose sur OpenLibrary. Une saisie manuelle est prévue en secours en cas d'indisponibilité.
    \item \textbf{Devise et territoire :} Tous les prix sont affichés en CHF ; la livraison est restreinte à la Suisse.
    \item \textbf{Coût minimal :} L'association ne doit pas supporter de frais d'abonnement mensuels élevés. PostFinance a été retenu pour ses frais de transaction transparents et son intégration avec la Poste Suisse.
\end{itemize}

\subsection{Risques identifiés}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{@{}p{4cm}p{2.5cm}p{6.5cm}@{}}
\toprule
\textbf{Risque} & \textbf{Impact} & \textbf{Action proposée} \\
\midrule
Livres anciens sans ISBN ou mal reconnus & Moyen & Prévoir une saisie manuelle complète en complément du scan \\
Lecture du code-barres lente ou imprécise & Moyen & Optimisation de l'algorithme de détection \\
Annonces incomplètes ou peu fiables & Élevé & Modération, photos obligatoires, champs standardisés \\
Sécurité insuffisante & Élevé & Authentification JWT, validation serveur, contrôle des permissions \\
Indisponibilité de l'API ISBN & Moyen & Mode de saisie manuelle intégré en secours \\
Demandes fonctionnelles excessives & Élevé & Application stricte du périmètre MVP défini avec la Product Owner \\
\bottomrule
\end{tabular}
\caption{Risques identifiés et actions proposées}
\label{tab:risques}
\end{table}

\subsection{Fonctionnalités hors périmètre (Phases 2 et 3)}

\begin{itemize}
    \item Système de messagerie entre acheteur et vendeur pour négocier le prix
    \item Gestion avancée des livraisons et étiquettes d'envoi
    \item Estimation automatique du prix de revente
    \item Recommandations intelligentes basées sur l'historique de navigation
    \item Avis et commentaires sur les livres
\end{itemize}

\section{Gestion du projet}

\subsection{Méthode Agile adoptée}

La méthode de gestion de projet retenue est une approche \textbf{Scrum simplifiée}, adaptée aux contraintes d'un développeur solo travaillant en parallèle de sa formation en apprentissage. Les sprints ont une durée de \textbf{2 semaines}, avec des plages de développement le soir (17h–20h), trois jours par semaine, soit environ \textbf{18 heures de développement effectif par sprint}.

Le suivi des tâches a été réalisé via \textbf{Microsoft Planner}, permettant de visualiser le backlog, les tâches en cours et les tâches terminées sous forme de tableau Kanban.

\vspace{8pt}
\noindent\textbf{Rôles Scrum adaptés :}
\begin{itemize}
    \item \textbf{Product Owner :} La bénévole de la recyclerie Caritas, qui exprimait ses besoins lors de réunions informelles et de démos en fin de sprint.
    \item \textbf{Développeur :} Alexandre REY, seul contributeur technique du projet.
    \item \textbf{Scrum Master :} Rôle assumé par le développeur lui-même, avec auto-organisation et ajustement régulier des priorités.
\end{itemize}

\subsection{Suivi Microsoft Planner}



\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{Planning_MVP1_caribooks.png}
    \caption{Dashboard de suivi du projet sur Microsoft Planner}
    \label{fig:placeholder}
\end{figure}


\subsection{Planning des sprints}

Le tableau ci-dessous présente le découpage en sprints du projet, planifié sur 12 semaines (6 sprints de 2 semaines) :

\begin{longtable}{@{}p{1.5cm}p{2.5cm}p{5.5cm}p{4.5cm}@{}}
\toprule
\textbf{Sprint} & \textbf{Dates} & \textbf{Objectifs} & \textbf{Livrable} \\
\midrule
\endhead
Sprint 1 & Semaines 1–2 &
  Mise en place de l'environnement (Docker, GitHub, CI/CD), modélisation de la base de données MySQL, création du DDL, configuration FastAPI de base &
  Environnement de dev opérationnel, schéma BDD validé \\[6pt]
Sprint 2 & Semaines 3–4 &
  Authentification : inscription, connexion, JWT, rôles (admin / user), hashage bcrypt, protection des routes &
  Endpoints \texttt{/auth/register} et \texttt{/auth/token} fonctionnels, middleware JWT \\[6pt]
Sprint 3 & Semaines 5–6 &
  Module scan ISBN : intégration librairie de scan dans le frontend, appel API OpenLibrary, préremplissage du formulaire, saisie manuelle en secours &
  Formulaire d'ajout de livre opérationnel, scan ISBN fonctionnel \\[6pt]
Sprint 4 & Semaines 7–8 &
  Catalogue et recherche : affichage des livres disponibles, filtres (titre, auteur, catégorie, état, prix), fiche détaillée, liste de favoris &
  Pages catalogue et fiche livre, moteur de recherche avec filtres \\[6pt]
Sprint 5 & Semaines 9–10 &
  Intégration PostFinance : tunnel de paiement, gestion des webhooks, mise à jour des statuts de commande, tests sur sandbox PostFinance &
  Paiement CHF fonctionnel (TWINT + carte) en environnement de test \\[6pt]
Sprint 6 & Semaines 11–12 &
  Back-office bénévole, tests unitaires et d'intégration (pytest), corrections de bugs, optimisations de performance, préparation du déploiement &
  Application complète testée et déployée (Vercel + Oracle Cloud) \\
\bottomrule
\caption{Planning des sprints CARIBOOKS}
\label{tab:sprints}
\end{longtable}

\section{Modélisation}

\subsection{Diagramme de contexte}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[node distance=15mm and 30mm]
        \node[syst] (sys) {Plateforme\\\textbf{CARIBOOKS}};
        \node[box, above left=of sys]  (ach)   {Acheteur};
        \node[box, left=of sys]        (vend)  {Vendeur\\(bénévole)};
        \node[box, below left=of sys]  (admin) {Administrateur};
        \node[extbox, above right=of sys] (ol)   {API OpenLibrary\\/ Google Books};
        \node[extbox, right=of sys]       (pf)   {PostFinance\\(paiement CHF)};
        \node[extbox, below right=of sys] (post) {Poste Suisse\\(livraison)};
        \draw[flow] (ach)   -- node[flowlab]{recherche, achat} (sys);
        \draw[flow] (vend)  -- node[flowlab]{dépôt d'annonce} (sys);
        \draw[flow] (admin) -- node[flowlab]{modération} (sys);
        \draw[flow] (sys) -- node[flowlab]{lookup ISBN} (ol);
        \draw[flow] (sys) -- node[flowlab]{paiement} (pf);
        \draw[flow] (sys) -- node[flowlab]{expédition} (post);
    \end{tikzpicture}
    \caption{Diagramme de contexte — CARIBOOKS et ses acteurs externes}
    \label{fig:contexte}
\end{figure}

\noindent Le diagramme de contexte positionne CARIBOOKS au centre de ses interactions avec les acteurs externes : les utilisateurs (acheteurs, vendeurs bénévoles, administrateurs), l'API ISBN externe (OpenLibrary), le prestataire de paiement (PostFinance) et le service de livraison (Poste Suisse).

\subsection{Diagramme de cas d'utilisation}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}
        \node[align=center] (vend) at (0,0) {\umlactor{Vendeur\\(bénévole)}};
        \node[ucsystem, minimum width=66mm, minimum height=72mm,
              label=above:{\textbf{Ajout d'un livre}}] (sysbox) at (5.6,0) {};
        \node[usecase] (u1) at (5.6,2.6)  {Scanner le\\code-barres ISBN};
        \node[usecase] (u2) at (5.6,0.9)  {Récupérer les\\métadonnées};
        \node[usecase] (u3) at (5.6,-0.8) {Saisir manuellement\\les informations};
        \node[usecase] (u4) at (5.6,-2.5) {Compléter état \& prix,\\publier l'annonce};
        \node[align=center] (ol) at (11.2,0.9) {\umlactor{API\\OpenLibrary}};
        \draw (vend) -- (u1);
        \draw (vend) -- (u3);
        \draw (vend) -- (u4);
        \draw[dep] (u1) -- node[flowlab]{\guillemotleft include\guillemotright} (u2);
        \draw[dep] (u3) -- node[flowlab, sloped]{\guillemotleft extend\guillemotright} (u1);
        \draw (u2) -- (ol);
    \end{tikzpicture}
    \caption{Diagramme des cas d'utilisation — Flux Scan ISBN}
    \label{fig:use_case_isbn}
\end{figure}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}
        \node[align=center] (ach) at (0,0) {\umlactor{Acheteur}};
        \node[ucsystem, minimum width=66mm, minimum height=72mm,
              label=above:{\textbf{Tunnel de paiement}}] (sysbox) at (5.6,0) {};
        \node[usecase] (p1) at (5.6,2.6)  {Valider le panier};
        \node[usecase] (p2) at (5.6,0.9)  {Choisir le moyen\\(TWINT / carte)};
        \node[usecase] (p3) at (5.6,-0.8) {Initier le\\paiement};
        \node[usecase] (p4) at (5.6,-2.5) {Recevoir la confirmation\\(webhook)};
        \node[align=center] (pf) at (11.2,0.05) {\umlactor{PostFinance}};
        \draw (ach) -- (p1);
        \draw (ach) -- (p2);
        \draw (ach) -- (p3);
        \draw[dep] (p3) -- node[flowlab]{\guillemotleft include\guillemotright} (p4);
        \draw (p3) -- (pf);
        \draw (p4) -- (pf);
    \end{tikzpicture}
    \caption{Diagramme des cas d'utilisation — Flux de paiement PostFinance}
    \label{fig:use_case_payment}
\end{figure}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
        % Acteurs
        \node[align=center] (ach)   at (0,2.4)  {\umlactor{Acheteur}};
        \node[align=center] (vend)  at (0,-1.2) {\umlactor{Vendeur}};
        \node[align=center] (admin) at (0,-4.2) {\umlactor{Administrateur}};
        \node[align=center] (ol) at (13,3.5)  {\umlactor{OpenLibrary}};
        \node[align=center] (pf) at (13,-3)   {\umlactor{PostFinance}};
        % Système
        \node[ucsystem, minimum width=86mm, minimum height=104mm,
              label=above:{\textbf{Système CARIBOOKS}}] at (6.5,-0.6) {};
        \node[usecase] (uc1) at (6.5,4.0)  {S'inscrire /\\se connecter};
        \node[usecase] (uc2) at (6.5,2.5)  {Parcourir le\\catalogue};
        \node[usecase] (uc3) at (6.5,1.0)  {Rechercher /\\filtrer};
        \node[usecase] (uc4) at (6.5,-0.5) {Gérer ses\\favoris};
        \node[usecase] (uc5) at (6.5,-2.0) {Passer commande\\\& payer};
        \node[usecase] (uc6) at (6.5,-3.5) {Ajouter un livre\\(scan ISBN)};
        \node[usecase] (uc7) at (6.5,-5.0) {Gérer ses\\annonces};
        \node[usecase] (uc8) at (6.5,-6.5) {Modérer / gérer\\les utilisateurs};
        % Liens acheteur
        \draw (ach) -- (uc1); \draw (ach) -- (uc2); \draw (ach) -- (uc3);
        \draw (ach) -- (uc4); \draw (ach) -- (uc5);
        % Liens vendeur
        \draw (vend) -- (uc1); \draw (vend) -- (uc6); \draw (vend) -- (uc7);
        % Liens admin
        \draw (admin) -- (uc8); \draw (admin) -- (uc7);
        % Acteurs externes
        \draw (uc6) -- (ol);
        \draw (uc5) -- (pf);
    \end{tikzpicture}}
    \caption{Diagramme des cas d'utilisation — Vue globale du système}
    \label{fig:use_case_global}
\end{figure}

\subsection{Diagramme d'activités}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[node distance=8mm]
        \node[init] (start) {};
        \node[act, below=of start] (a1) {Parcourir le catalogue};
        \node[act, below=of a1] (a2) {Rechercher / filtrer\\(titre, auteur, prix, état)};
        \node[act, below=of a2] (a3) {Consulter la fiche du livre};
        \node[act, below=of a3] (a4) {Ajouter au panier};
        \node[decision, below=of a4] (d1) {Connecté ?};
        \node[act, left=18mm of d1] (a5) {Se connecter /\\s'inscrire};
        \node[act, below=14mm of d1] (a6) {Valider le panier};
        \node[act, below=of a6] (a7) {Paiement PostFinance\\(TWINT / carte, CHF)};
        \node[decision, below=of a7] (d2) {Paiement\\réussi ?};
        \node[act, right=16mm of d2] (a8) {Afficher l'erreur};
        \node[act, below=14mm of d2] (a9) {Commande confirmée\\(statut PAYÉE)};
        \node[final, below=of a9] (end) {};
        \draw[flow] (start) -- (a1);
        \draw[flow] (a1) -- (a2);
        \draw[flow] (a2) -- (a3);
        \draw[flow] (a3) -- (a4);
        \draw[flow] (a4) -- (d1);
        \draw[flow] (d1) -- node[flowlab]{non} (a5);
        \draw[flow] (a5) |- (a6);
        \draw[flow] (d1) -- node[flowlab]{oui} (a6);
        \draw[flow] (a6) -- (a7);
        \draw[flow] (a7) -- (d2);
        \draw[flow] (d2) -- node[flowlab]{non} (a8);
        \draw[flow] (a8) |- (a6);
        \draw[flow] (d2) -- node[flowlab]{oui} (a9);
        \draw[flow] (a9) -- (end);
    \end{tikzpicture}
    \caption{Diagramme d'activités — Parcours d'achat complet}
    \label{fig:activites_achat}
\end{figure}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[node distance=8mm]
        \node[init] (start) {};
        \node[act, below=of start] (a1) {Scanner le code-barres ISBN};
        \node[decision, below=of a1] (d1) {Code\\reconnu ?};
        \node[act, below=14mm of d1] (a2) {Appel API OpenLibrary\\(GET /isbn/\{isbn\})};
        \node[decision, below=of a2] (d2) {Métadonnées\\trouvées ?};
        \node[act, right=18mm of d2] (a3) {Saisie manuelle\\des informations};
        \node[act, below=14mm of d2] (a4) {Préremplir le formulaire\\(titre, auteur, éditeur)};
        \node[act, below=of a4] (a5) {Compléter état \& prix (CHF)};
        \node[act, below=of a5] (a6) {Publier l'annonce};
        \node[act, below=of a6] (a7) {Enregistrer le scan\\(table scan\_isbn)};
        \node[final, below=of a7] (end) {};
        \draw[flow] (start) -- (a1);
        \draw[flow] (a1) -- (d1);
        \draw[flow] (d1) -- node[flowlab]{oui} (a2);
        \draw[flow] (d1) -| node[flowlab,pos=0.25]{non} (a3);
        \draw[flow] (a2) -- (d2);
        \draw[flow] (d2) -- node[flowlab]{non} (a3);
        \draw[flow] (a3) |- (a5);
        \draw[flow] (d2) -- node[flowlab]{oui} (a4);
        \draw[flow] (a4) -- (a5);
        \draw[flow] (a5) -- (a6);
        \draw[flow] (a6) -- (a7);
        \draw[flow] (a7) -- (end);
    \end{tikzpicture}
    \caption{Diagramme d'activités — Flux d'ajout d'un livre via scan ISBN}
    \label{fig:activites_isbn}
\end{figure}

\subsection{Diagramme de séquence}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
        % En-têtes des participants
        \node[actorbox] (ben) at (0,0)    {Bénévole};
        \node[actorbox] (nav) at (3.6,0)  {Navigateur\\(scanner JS)};
        \node[actorbox] (api) at (7.6,0)  {API FastAPI};
        \node[actorbox] (ol)  at (11,0)   {OpenLibrary\\/ Google Books};
        \node[actorbox] (bd)  at (14.2,0) {BD MySQL};
        % Lignes de vie
        \foreach \p in {ben,nav,api,ol,bd} \draw[lifeline] (\p.south) -- ++(0,-8.6);
        % Messages
        \draw[msg] ($(ben)+(0,-1)$) -- node[above]{scanne le code-barres} ($(nav)+(0,-1)$);
        \draw[msg] ($(nav)+(0,-1.8)$) -- ++(0.9,0) -- ++(0,-0.4) -- ++(-0.9,0)
            node[pos=0.5, right=9mm, font=\scriptsize]{décode l'ISBN};
        \draw[msg] ($(nav)+(0,-3)$) -- node[above]{GET /isbn/\{isbn\}} ($(api)+(0,-3)$);
        \draw[msg] ($(api)+(0,-3.8)$) -- node[above]{requête métadonnées} ($(ol)+(0,-3.8)$);
        \draw[retmsg] ($(ol)+(0,-4.8)$) -- node[above]{titre, auteur, éditeur} ($(api)+(0,-4.8)$);
        \draw[retmsg] ($(api)+(0,-5.6)$) -- node[above]{JSON métadonnées} ($(nav)+(0,-5.6)$);
        \draw[msg] ($(ben)+(0,-6.4)$) -- node[above]{complète état + prix} ($(nav)+(0,-6.4)$);
        \draw[msg] ($(nav)+(0,-7.2)$) -- node[above]{POST /articles + /scans} ($(api)+(0,-7.2)$);
        \draw[msg] ($(api)+(0,-8)$) -- node[above]{INSERT livre / scan\_isbn} ($(bd)+(0,-8)$);
    \end{tikzpicture}}
    \caption{Diagramme de séquence — Scan ISBN et préremplissage des métadonnées}
    \label{fig:sequence_isbn}
\end{figure}

\noindent Le diagramme de séquence du scan ISBN illustre l'enchaînement suivant : le bénévole scanne le code-barres depuis le navigateur → la librairie de décodage extrait l'ISBN → une requête est envoyée à l'API FastAPI → le backend réutilise les métadonnées OpenLibrary si disponibles → les métadonnées sont retournées au frontend pour préremplissage → le bénévole complète l'état et le prix → publication de l'annonce.

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
        \node[actorbox] (ach) at (0,0)    {Acheteur};
        \node[actorbox] (fro) at (3.4,0)  {Frontend\\Next.js};
        \node[actorbox] (api) at (7.2,0)  {API FastAPI};
        \node[actorbox] (pf)  at (11,0)   {PostFinance};
        \node[actorbox] (bd)  at (14.2,0) {BD MySQL};
        \foreach \p in {ach,fro,api,pf,bd} \draw[lifeline] (\p.south) -- ++(0,-9.4);
        \draw[msg] ($(ach)+(0,-1)$) -- node[above]{valide le panier} ($(fro)+(0,-1)$);
        \draw[msg] ($(fro)+(0,-1.8)$) -- node[above]{POST /orders} ($(api)+(0,-1.8)$);
        \draw[msg] ($(api)+(0,-2.6)$) -- node[above]{INSERT commande (EN\_ATTENTE)} ($(bd)+(0,-2.6)$);
        \draw[msg] ($(api)+(0,-3.6)$) -- node[above]{crée la transaction} ($(pf)+(0,-3.6)$);
        \draw[retmsg] ($(pf)+(0,-4.4)$) -- node[above]{URL de paiement} ($(api)+(0,-4.4)$);
        \draw[retmsg] ($(api)+(0,-5.2)$) -- node[above]{redirection} ($(fro)+(0,-5.2)$);
        \draw[msg] ($(fro)+(0,-6)$) -- node[above, font=\scriptsize]{paye (TWINT / carte)} ($(pf)+(0,-6)$);
        \draw[msg] ($(pf)+(0,-6.8)$) -- node[above]{webhook succès / échec} ($(api)+(0,-6.8)$);
        \draw[msg] ($(api)+(0,-7.6)$) -- node[above]{UPDATE statut (PAYÉE)} ($(bd)+(0,-7.6)$);
        \draw[retmsg] ($(api)+(0,-8.6)$) -- node[above]{confirmation} ($(fro)+(0,-8.6)$);
        \draw[retmsg] ($(fro)+(0,-9.2)$) -- node[above]{page de confirmation} ($(ach)+(0,-9.2)$);
    \end{tikzpicture}}
    \caption{Diagramme de séquence — Tunnel de paiement PostFinance}
    \label{fig:sequence_paiement}
\end{figure}

\begin{figure}[h]
    \centering
    \resizebox{0.92\textwidth}{!}{%
    \begin{tikzpicture}
        \node[actorbox] (usr) at (0,0)   {Utilisateur};
        \node[actorbox] (fro) at (4,0)   {Frontend\\Next.js};
        \node[actorbox] (api) at (8.5,0) {API FastAPI};
        \node[actorbox] (bd)  at (12.5,0){BD MySQL};
        \foreach \p in {usr,fro,api,bd} \draw[lifeline] (\p.south) -- ++(0,-7.6);
        \draw[msg] ($(usr)+(0,-1)$) -- node[above]{saisit email + mot de passe} ($(fro)+(0,-1)$);
        \draw[msg] ($(fro)+(0,-1.8)$) -- node[above]{POST /auth/token} ($(api)+(0,-1.8)$);
        \draw[msg] ($(api)+(0,-2.6)$) -- node[above]{SELECT utilisateur} ($(bd)+(0,-2.6)$);
        \draw[retmsg] ($(bd)+(0,-3.4)$) -- node[above]{hash bcrypt} ($(api)+(0,-3.4)$);
        \draw[msg] ($(api)+(0,-4.2)$) -- ++(1,0) -- ++(0,-0.4) -- ++(-1,0)
            node[pos=0.5, right=10mm, font=\scriptsize]{vérifie (passlib) + signe JWT (HS256)};
        \draw[retmsg] ($(api)+(0,-5.4)$) -- node[above]{access\_token (JWT)} ($(fro)+(0,-5.4)$);
        \draw[retmsg] ($(fro)+(0,-6.2)$) -- node[above]{connexion établie} ($(usr)+(0,-6.2)$);
        \draw[msg] ($(fro)+(0,-7)$) -- node[above]{requêtes : Authorization: Bearer} ($(api)+(0,-7)$);
    \end{tikzpicture}}
    \caption{Diagramme de séquence — Authentification JWT}
    \label{fig:sequence_auth}
\end{figure}

\noindent Le diagramme de séquence d'authentification décrit le flux suivant : l'utilisateur soumet ses identifiants → FastAPI valide l'email/mot de passe en base (SQLAlchemy + bcrypt) → si valide, un JWT signé est généré et retourné → le frontend stocke le token → chaque requête protégée envoie le token dans l'en-tête \texttt{Authorization: Bearer <token>} → le middleware FastAPI vérifie la signature et extrait le rôle.

\subsection{Diagramme de classes}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
    \begin{scope}[every node/.append style={umlclass, rectangle split parts=2, text width=33mm}]
        % Colonne gauche
        \node (typeobjet) at (0,4.6)  {\textbf{TypeObjet}\nodepart{second}- id\_type\_objet\\- libelle\\- code};
        \node (etat)      at (0,1.6)  {\textbf{EtatUsure}\nodepart{second}- id\_etat\_usure\\- libelle};
        \node (cat)       at (0,-1.4) {\textbf{Categorie}\nodepart{second}- id\_categorie\\- libelle};
        \node (source)    at (0,-4.4) {\textbf{SourceStock}\nodepart{second}- id\_source\_stock\\- libelle\\- type\_source};
        % Colonne centrale
        \node (article) at (5.2,3.2)  {\textbf{Article}\nodepart{second}- id\_article\\- sku\\- titre\\- prix\_chf\\- actif};
        \node (livre)   at (5.2,-0.6) {\textbf{Livre}\nodepart{second}- isbn\\- auteur\\- editeur\\- date\_publication\\- langue};
        \node (stock)   at (5.2,-4.4) {\textbf{Stock}\nodepart{second}- id\_stock\\- quantite\_disponible\\- quantite\_reservee};
        \node (scan)    at (5.2,-7.6) {\textbf{ScanISBN}\nodepart{second}- id\_scan\_isbn\\- isbn\_lu\\- valide};
        % Colonne droite
        \node (user)  at (10.6,4.4)  {\textbf{Utilisateur}\nodepart{second}- id\_utilisateur\\- email\\- mot\_de\_passe\_hash\\- role};
        \node (cmd)   at (10.6,1.2)  {\textbf{Commande}\nodepart{second}- id\_commande\\- numero\_commande\\- montant\_total\_chf\\- statut};
        \node (ligne) at (10.6,-2.2) {\textbf{LigneCommande}\nodepart{second}- id\_ligne\_commande\\- quantite\\- prix\_unitaire\_chf};
        \node (paie)  at (10.6,-5.4) {\textbf{Paiement}\nodepart{second}- id\_paiement\\- reference\_externe\\- montant\_chf\\- statut};
    \end{scope}
        % Héritage Livre -> Article
        \draw[-{Triangle[open, length=3.5mm, width=4mm]}, thick] (livre) -- (article);
        % Associations
        \draw (article) -- node[card, near start]{*} node[card, near end]{1} (typeobjet);
        \draw (article) -- node[card, near start]{*} node[card, near end]{1} (etat);
        \draw (article) -- node[card, midway, above, font=\scriptsize]{* \quad article\_categorie \quad *} (cat);
        \draw (stock) -- node[card, near start]{*} node[card, near end]{1} (article);
        \draw (stock) -- node[card, near start]{*} node[card, near end]{1} (source);
        \draw (cmd) -- node[card, near start]{*} node[card, near end]{1} (user);
        \draw (ligne) -- node[card, near start]{*} node[card, near end]{1} (cmd);
        \draw (ligne) -- node[card, near start]{*} node[card, near end]{1} (article);
        \draw (paie) -- node[card, near start]{1} node[card, near end]{1} (cmd);
        \draw (scan) -- node[card, near start]{*} node[card, near end]{1} (user);
        \draw (scan) -- node[card, near start]{*} node[card, near end]{1} (livre);
    \end{tikzpicture}}
    \caption{Diagramme de classes — Modèle objet CARIBOOKS}
    \label{fig:classes}
\end{figure}

\noindent Le diagramme de classes présente les entités principales du domaine métier : \texttt{Utilisateur}, \texttt{Article}, \texttt{Livre} (sous-classe d'\texttt{Article}), \texttt{Categorie}, \texttt{EtatUsure}, \texttt{TypeObjet}, \texttt{Stock}, \texttt{Commande}, \texttt{LigneCommande}, \texttt{Paiement}, \texttt{ScanIsbn}. La relation d'héritage entre \texttt{Article} et \texttt{Livre} reflète la table \texttt{livre} liée à la table \texttt{article} par une clé primaire partagée.

\subsection{Diagramme d'objets}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[every node/.append style={umlobject, text width=44mm}]
        \node (u) at (0,0) {\underline{\textbf{marie : Utilisateur}}\nodepart{second}email = marie@example.ch\\role = "user"};
        \node (c) at (6,0) {\underline{\textbf{cmd1024 : Commande}}\nodepart{second}numero = "CMD-2026-1024"\\montant\_total\_chf = 27.50\\statut = "PAYÉE"};
        \node (p) at (12,0) {\underline{\textbf{paie1 : Paiement}}\nodepart{second}fournisseur = "POSTFINANCE"\\montant\_chf = 27.50\\statut = "RÉUSSI"};
        \node (l1) at (3,-4) {\underline{\textbf{l1 : LigneCommande}}\nodepart{second}quantite = 1\\prix\_unitaire\_chf = 12.50};
        \node (l2) at (9,-4) {\underline{\textbf{l2 : LigneCommande}}\nodepart{second}quantite = 1\\prix\_unitaire\_chf = 15.00};
        \node (a1) at (3,-8) {\underline{\textbf{a1 : Livre}}\nodepart{second}titre = "L'Étranger"\\isbn = "9782070360024"\\prix\_chf = 12.50};
        \node (a2) at (9,-8) {\underline{\textbf{a2 : Livre}}\nodepart{second}titre = "1984"\\isbn = "9780451524935"\\prix\_chf = 15.00};
        \draw (u) -- (c);
        \draw (c) -- (p);
        \draw (c) -- (l1);
        \draw (c) -- (l2);
        \draw (l1) -- (a1);
        \draw (l2) -- (a2);
    \end{tikzpicture}}
    \caption{Diagramme d'objets — Exemple d'une commande en cours}
    \label{fig:objets}
\end{figure}

\subsection{Diagramme de composants}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[node distance=10mm]
        % Frontend
        \node[comp, fill=cyan!10, minimum width=44mm, minimum height=20mm] (front)
            {\textbf{Frontend Next.js}\\\scriptsize Pages, composants React,\\\scriptsize services API, scan ISBN (JS)};
        % API
        \node[comp, fill=blue!10, below=18mm of front, minimum width=70mm, minimum height=26mm] (api)
            {\textbf{API FastAPI}\\[2pt]\scriptsize presentation (routers, schemas Pydantic)\\\scriptsize services (JWT, paiement, books)\\\scriptsize infrastructure (SQLAlchemy, CRUD)};
        % BD
        \node[comp, fill=orange!12, below=14mm of api, minimum width=44mm] (db)
            {\textbf{Base de données MySQL}};
        % Services externes
        \node[extbox, right=24mm of api, minimum width=34mm, minimum height=26mm, align=center] (ext)
            {\textbf{Services externes}\\[3pt]\scriptsize OpenLibrary\\\scriptsize PostFinance\\\scriptsize Poste Suisse};
        \draw[dep] (front) -- node[flowlab]{HTTP / REST (JSON)} (api);
        \draw[dep] (api) -- node[flowlab]{ORM SQLAlchemy} (db);
        \draw[dep] (api) -- node[flowlab]{HTTPS} (ext);
    \end{tikzpicture}
    \caption{Diagramme de composants — Architecture logicielle CARIBOOKS}
    \label{fig:composants}
\end{figure}

\noindent Le diagramme de composants illustre les quatre composants principaux de l'application : le \textbf{Frontend Next.js} (composants React, pages, services API), l'\textbf{API FastAPI} (routers, services, modèles Pydantic, couche SQLAlchemy), la \textbf{Base de données MySQL}, et les \textbf{Services externes} (OpenLibrary API, PostFinance API, Poste Suisse). Les dépendances entre composants respectent le principe de séparation des préoccupations.

\subsection{Modélisation de la base de données (MCD / MLD)}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
        % Entités
        \node[entite] (type)   at (0,1.8)   {\textbf{TYPE\_OBJET}\nodepart{second}\underline{id\_type\_objet}\\libelle, code};
        \node[entite] (article)at (4.5,0)    {\textbf{ARTICLE}\nodepart{second}\underline{id\_article}\\sku, titre, prix\_chf};
        \node[entite] (etat)   at (9,1.8)    {\textbf{ETAT\_USURE}\nodepart{second}\underline{id\_etat\_usure}\\libelle};
        \node[entite] (cat)    at (9,-1.8)   {\textbf{CATEGORIE}\nodepart{second}\underline{id\_categorie}\\libelle};
        \node[entite] (source) at (0,-1.8)   {\textbf{SOURCE\_STOCK}\nodepart{second}\underline{id\_source\_stock}\\libelle, type\_source};
        \node[entite] (livre)  at (4.5,-4)    {\textbf{LIVRE}\nodepart{second}isbn, auteur,\\editeur, langue};
        \node[entite] (user)   at (0,-7)     {\textbf{UTILISATEUR}\nodepart{second}\underline{id\_utilisateur}\\email, role};
        \node[entite] (cmd)    at (5,-7)      {\textbf{COMMANDE}\nodepart{second}\underline{id\_commande}\\numero, montant, statut};
        \node[entite] (paie)   at (10,-7)     {\textbf{PAIEMENT}\nodepart{second}\underline{id\_paiement}\\reference, montant};
        % Associations
        \node[assoc] (estt) at (2.1,1)   {est de type};
        \node[assoc] (aet)  at (6.7,1)   {a pour état};
        \node[assoc] (cla)  at (6.7,-1)  {classé dans};
        \node[assoc] (sto)  at (2.1,-1)  {stocké\\(quantité)};
        \node[assoc] (estu) at (4.5,-2)  {est un};
        \node[assoc] (pas)  at (2.5,-7)  {passe};
        \node[assoc] (reg)  at (7.5,-7)  {réglée par};
        \node[assoc] (con)  at (5,-3.4)  {contient\\(qté, prix)};
        \node[assoc] (sca)  at (1.6,-4.6){scanne};
        % Liens + cardinalités
        \draw (article) -- node[card,near start]{(1,1)} node[card,near end]{(0,n)} (estt) -- (type);
        \draw (article) -- node[card,near start]{(1,1)} node[card,near end]{(0,n)} (aet) -- (etat);
        \draw (article) -- node[card,near start]{(0,n)} node[card,near end]{(0,n)} (cla) -- (cat);
        \draw (article) -- node[card,near start]{(1,n)} node[card,near end]{(0,n)} (sto) -- (source);
        \draw (article) -- node[card,near start]{(0,1)} node[card,near end]{(1,1)} (estu) -- (livre);
        \draw (user) -- node[card,near start]{(0,n)} node[card,near end]{(1,1)} (pas) -- (cmd);
        \draw (cmd)  -- node[card,near start]{(1,1)} node[card,near end]{(1,1)} (reg) -- (paie);
        \draw (cmd)  -- node[card,near start]{(1,n)} node[card,near end]{(0,n)} (con) -- (article);
        \draw (user) -- node[card,near start]{(0,n)} node[card,near end]{(0,n)} (sca) -- (livre);
    \end{tikzpicture}}
    \caption{Modèle Conceptuel de Données (MCD) — MySQL}
    \label{fig:mcd}
\end{figure}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[every node/.append style={table, text width=40mm}]
        % Colonne 1 : référentiels
        \node (type)   at (0,0)   {\textbf{type\_objet}\nodepart{second}\underline{id\_type\_objet}\\libelle, code, description};
        \node (etat)   at (0,-4)  {\textbf{etat\_usure}\nodepart{second}\underline{id\_etat\_usure}\\libelle, description};
        \node (cat)    at (0,-8)  {\textbf{categorie}\nodepart{second}\underline{id\_categorie}\\libelle, description};
        \node (source) at (0,-12) {\textbf{source\_stock}\nodepart{second}\underline{id\_source\_stock}\\libelle, type\_source};
        % Colonne 2 : articles
        \node (article) at (6,-1)  {\textbf{article}\nodepart{second}\underline{id\_article}\\\# id\_type\_objet\\\# id\_etat\_usure\\sku, titre, prix\_chf, actif};
        \node (livre)   at (6,-5)  {\textbf{livre}\nodepart{second}\underline{\# id\_article}\\isbn, auteur, editeur\\date\_publication, langue};
        \node (artcat)  at (6,-9)  {\textbf{article\_categorie}\nodepart{second}\underline{\# id\_article}\\\underline{\# id\_categorie}};
        \node (stock)   at (6,-13) {\textbf{stock}\nodepart{second}\underline{id\_stock}\\\# id\_article\\\# id\_source\_stock\\quantite\_disponible, reservee};
        % Colonne 3 : commandes
        \node (user)  at (12,0)   {\textbf{utilisateur}\nodepart{second}\underline{id\_utilisateur}\\nom, prenom, email\\mot\_de\_passe\_hash, role};
        \node (cmd)   at (12,-4)  {\textbf{commande}\nodepart{second}\underline{id\_commande}\\\# id\_utilisateur\\numero, montant\_total\_chf, statut};
        \node (ligne) at (12,-8)  {\textbf{ligne\_commande}\nodepart{second}\underline{id\_ligne\_commande}\\\# id\_commande\\\# id\_article\\quantite, prix\_unitaire\_chf};
        \node (scan)  at (12,-12) {\textbf{scan\_isbn}\nodepart{second}\underline{id\_scan\_isbn}\\\# id\_utilisateur\\\# id\_article\_livre\\isbn\_lu, valide};
        % Colonne 4 : paiement
        \node (paie)  at (18,-4)  {\textbf{paiement}\nodepart{second}\underline{id\_paiement}\\\# id\_commande\\reference\_externe, montant\_chf, statut};
        % Clés étrangères
        \draw[flow] (article.west) -- (type.east);
        \draw[flow] (article.west) -- (etat.east);
        \draw[flow] (livre.north) -- (article.south);
        \draw[flow] (artcat.west) -- (cat.east);
        \draw[flow] (artcat.north) -- (article.south);
        \draw[flow] (stock.west) -- (source.east);
        \draw[flow] (stock.north) -- (article.south);
        \draw[flow] (cmd.north) -- (user.south);
        \draw[flow] (ligne.north) -- (cmd.south);
        \draw[flow] (ligne.west) -- (article.east);
        \draw[flow] (scan.west) -- (livre.east);
        \draw[flow] (paie.north) to[out=90,in=0] (cmd.east);
    \end{tikzpicture}}
    \caption{Modèle Logique de Données (MLD)}
    \label{fig:mld}
\end{figure}

\noindent La base de données CARIBOOKS repose sur \textbf{13 tables} organisées autour d'une architecture en héritage de table (\textit{table per class}) pour les articles : la table \texttt{article} contient les attributs communs à tout objet en vente, et la table \texttt{livre} étend \texttt{article} avec les attributs spécifiques aux livres (ISBN, auteur, éditeur, date de publication, langue). Ce design permet d'étendre la plateforme à d'autres types d'objets (vêtements, jouets, etc.) sans restructurer le schéma de base.

\noindent Les principales relations sont :
\begin{itemize}
    \item \texttt{article} $\leftrightarrow$ \texttt{livre} : héritage 1-1 (clé primaire partagée)
    \item \texttt{article} $\leftrightarrow$ \texttt{categorie} : association N-N via \texttt{article\_categorie}
    \item \texttt{article} $\leftrightarrow$ \texttt{stock} : 1-N par source de stock
    \item \texttt{utilisateur} $\leftrightarrow$ \texttt{commande} : 1-N
    \item \texttt{commande} $\leftrightarrow$ \texttt{ligne\_commande} $\leftrightarrow$ \texttt{article} : N-N décomposée
    \item \texttt{commande} $\leftrightarrow$ \texttt{paiement} : 1-1
    \item \texttt{utilisateur} $\leftrightarrow$ \texttt{scan\_isbn} $\leftrightarrow$ \texttt{livre} : traçabilité des scans
\end{itemize}

\subsection{Architecture technique}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[node distance=12mm]
        \node[layer, fill=gray!12] (client) {\textbf{Client}\\\scriptsize Navigateur web / mobile};
        \node[layer, fill=cyan!12, below=of client] (front)
            {\textbf{Frontend — Next.js (React / TypeScript)}\\\scriptsize SSR/SSG · scan ISBN · déployé sur Vercel};
        \node[layer, fill=blue!12, below=of front] (api)
            {\textbf{Backend — API FastAPI (Python)}\\\scriptsize Pydantic · JWT · SQLAlchemy · Swagger};
        \node[layer, fill=orange!14, below=of api] (db)
            {\textbf{Base de données — MySQL}};
        % Services externes à droite
        \node[extbox, right=22mm of api, minimum width=34mm, minimum height=22mm, align=center] (ext)
            {\textbf{Services externes}\\[3pt]\scriptsize OpenLibrary / Google Books\\\scriptsize PostFinance (CHF)\\\scriptsize Poste Suisse};
        % Conteneurisation
        \node[draw, dashed, rounded corners, fit=(api)(db), inner sep=7mm,
              label=below:{\scriptsize Docker / docker-compose · CI-CD GitHub Actions}] (docker) {};
        \draw[flow] (client) -- node[flowlab]{HTTPS} (front);
        \draw[flow] (front) -- node[flowlab]{REST / JSON} (api);
        \draw[flow] (api) -- node[flowlab]{ORM} (db);
        \draw[flow] (api) -- node[flowlab]{HTTPS} (ext);
    \end{tikzpicture}
    \caption{Architecture technique — FastAPI / Next.js / MySQL / PostFinance / Docker}
    \label{fig:architecture}
\end{figure}

\begin{itemize}
    \item \textbf{Backend (API REST) :} Développé avec \textbf{FastAPI} (Python). Ce choix apporte de très hautes performances, la validation automatique des données grâce à Pydantic, et une documentation API automatique via Swagger. Une première tentative avec Express.js (Node.js) avait été envisagée, mais abandonnée au profit de FastAPI pour une meilleure maîtrise technique.
    \item \textbf{Frontend :} Développé avec \textbf{Next.js} (React / TypeScript). Ce framework permet un rendu côté serveur (SSR) ou statique (SSG), offrant d'excellentes performances SEO pour la vitrine en ligne, tout en proposant une interface fluide (SPA) pour le back-office des bénévoles.
    \item \textbf{Base de données :} \textbf{MySQL}. SGBD relationnel robuste, adapté à la structure de données du catalogue de livres.
    \item \textbf{ORM :} \textbf{SQLAlchemy}. Permet une abstraction de la base de données, des requêtes paramétrées automatiques (protection contre les injections SQL) et une gestion fluide des transactions.
    \item \textbf{Paiement :} Intégration de l'API \textbf{PostFinance}, prestataire suisse supportant TWINT, Visa et Mastercard en CHF, avec synergie native avec la Poste Suisse pour la livraison.
    \item \textbf{Conteneurisation :} L'ensemble de l'application est conteneurisé avec \textbf{Docker} et orchestré via \texttt{docker-compose}, garantissant la reproductibilité de l'environnement du développement au déploiement.
\end{itemize}

\subsection{Diagramme de déploiement (vue physique)}

Le diagramme de déploiement décrit la répartition physique des composants logiciels sur les n\oe{}uds d'exécution : le poste client (navigateur), le frontend hébergé sur \textbf{Vercel}, le backend conteneurisé (API FastAPI) et la base MySQL hébergés sur \textbf{Oracle Cloud}, et les services tiers consommés en HTTPS.

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[
        device/.style={rectangle, draw=black, thick, fill=gray!12, align=center, font=\small},
        artifact/.style={rectangle, draw=black, fill=blue!8, align=center, font=\scriptsize, inner sep=3pt}]
        \node[device, minimum width=32mm, minimum height=15mm,
              label=above:{\scriptsize\guillemotleft device\guillemotright}] (client) at (0,0)
              {\textbf{Poste client}\\\scriptsize Navigateur\\\scriptsize web / mobile};
        \node[device, minimum width=42mm, minimum height=16mm,
              label=above:{\scriptsize\guillemotleft cloud — Vercel\guillemotright}] (vercel) at (6,3.2) {};
        \node[artifact, minimum width=34mm] (fe) at (6,3.2) {Frontend Next.js\\\scriptsize SSR / SSG};
        \node[device, minimum width=48mm, minimum height=34mm,
              label=above:{\scriptsize\guillemotleft serveur — Oracle Cloud\guillemotright}] (host) at (6,-2) {};
        \node[artifact, minimum width=38mm] (api) at (6,-1.1) {API FastAPI (Uvicorn)\\\scriptsize VM Docker};
        \node[artifact, minimum width=38mm] (db) at (6,-3) {Base MySQL\\\scriptsize Oracle Cloud};
        \node[device, minimum width=34mm, minimum height=22mm,
              label=above:{\scriptsize\guillemotleft services externes\guillemotright}] (ext) at (12,0.6)
              {\scriptsize PostFinance\\[2pt]OpenLibrary /\\Google Books\\[2pt]Poste Suisse};
        \draw[flow] (client) -- node[flowlab]{HTTPS} (vercel);
        \draw[flow] (client) -- node[flowlab]{HTTPS / REST} (api);
        \draw[flow] (api) -- node[flowlab]{TCP 3306} (db);
        \draw[flow] (api) -- node[flowlab]{HTTPS} (ext);
    \end{tikzpicture}
    \caption{Diagramme de déploiement — infrastructure physique de CARIBOOKS}
    \label{fig:deploiement}
\end{figure}

\subsection{Conception UI/UX}

\includegraphics[with=\textwidth]{vue_telephone.png}

% --- CHAPITRE 3 ---
\chapter{Réalisation et mise en \oe{}uvre}

\section{Environnement et outils de développement}

\begin{itemize}
    \item \textbf{Système d'exploitation :} macOS (puce Intel)
    \item \textbf{Éditeur de code :} Visual Studio Code (VS Code) avec GitHub Copilot
    \item \textbf{Gestion de versions :} Git avec GitHub (licence GitHub for Students Pro)
    \item \textbf{Conteneurisation :} Docker — l'application complète (API + frontend + base de données MySQL) est conteneurisée via \texttt{docker-compose}
    \item \textbf{CI/CD :} Pipeline d'intégration et de déploiement continus via GitHub Actions
    \item \textbf{Test API :} Thunder Client (extension VS Code, alternative à Postman)
    \item \textbf{Documentation API :} Swagger UI (généré automatiquement par FastAPI)
    \item \textbf{Suivi de projet :} Microsoft Planner (tableau Kanban, backlog)
    \item \textbf{Dépôt GitHub :} \url{https://github.com/Visualstudiocodetest/plobooks}
\end{itemize}

\section{Technologies utilisées}

\begin{itemize}
    \item \textbf{Frontend :} Next.js (React) — TypeScript
    \item \textbf{Backend :} FastAPI (Python) — avec Pydantic pour la validation des données
    \item \textbf{ORM :} SQLAlchemy
    \item \textbf{Base de données :} MySQL
    \item \textbf{Authentification :} JWT (\texttt{python-jose}), hashage \texttt{bcrypt} via \texttt{passlib}
    \item \textbf{Paiement :} API PostFinance (TWINT, Visa/Mastercard en CHF)
    \item \textbf{API externe :} OpenLibrary (récupération des métadonnées ISBN)
    \item \textbf{Conteneurisation :} Docker + docker-compose
    \item \textbf{CI/CD :} GitHub Actions
\end{itemize}

\section{Présentation des développements réalisés}

\subsection{Organisation du code (architecture en couches)}

Le backend suit une \textbf{architecture en couches} inspirée des principes de l'architecture hexagonale, garantissant la séparation des préoccupations exigée par le bloc 2 du référentiel. Le code est organisé en quatre répertoires :

\begin{itemize}
    \item \texttt{presentation/} — couche de présentation : les \textit{routers} FastAPI (un par domaine fonctionnel : \texttt{auth\_router}, \texttt{book\_router}, \texttt{catalog\_router}, \texttt{article\_router}, \texttt{stock\_router}, \texttt{order\_router}, \texttt{scan\_router}, \texttt{user\_router}, \texttt{images\_router}), les schémas Pydantic de validation (\texttt{schemas.py}, \texttt{auth\_schemas.py}) et les dépendances d'injection (\texttt{deps.py}).
    \item \texttt{services/} — couche métier : logique applicative isolée des routes (\texttt{jwt\_service}, \texttt{payment\_service}, \texttt{postfinance\_service}, \texttt{book\_service}, \texttt{image\_service}).
    \item \texttt{infrastructure/} — couche d'accès aux données : configuration SQLAlchemy (\texttt{db.py}), modèles ORM (\texttt{models.py}) et opérations CRUD génériques et spécialisées (\texttt{crud\_base}, \texttt{crud\_user}, \texttt{crud\_book}).
    \item \texttt{domain/} — entités métier (\texttt{book.py}).
\end{itemize}

\noindent Le point d'entrée \texttt{main.py} instancie l'application FastAPI, enregistre l'ensemble des routers, configure le middleware \textbf{CORS} (origines autorisées : domaine Vercel de production et \texttt{localhost} en développement) et monte le service de fichiers statiques (\texttt{/static}) pour les images. Un endpoint \texttt{/health} est exposé pour les sondes de disponibilité (monitoring, load balancer). Le frontend Next.js est structuré classiquement en \texttt{pages}, \texttt{components}, \texttt{hooks} et services d'appel à l'API.

\subsection{Module authentification}

Le module d'authentification expose deux endpoints : \texttt{POST /auth/register} (création de compte, renvoie un \texttt{201} ou un \texttt{400} si l'email existe déjà) et \texttt{POST /auth/token} (connexion, convention OAuth2). À la connexion, l'utilisateur est récupéré par email via \texttt{crud\_user}, et le mot de passe est vérifié avec \texttt{passlib} (bcrypt). Si les identifiants sont valides, un JWT signé en \textbf{HS256} est généré (\texttt{jwt\_service.create\_access\_token}), contenant l'email (\texttt{sub}), le rôle et une expiration de \textbf{480 minutes}. La protection des routes repose sur des dépendances FastAPI : \texttt{get\_current\_user} (décode et valide le token via le schéma \texttt{HTTPBearer}), \texttt{require\_admin} et \texttt{require\_user} (contrôle du rôle). En environnement de production, l'application refuse de démarrer si la \texttt{SECRET\_KEY} n'est pas configurée de façon sécurisée.

\subsection{Module catalogue, articles et stock}

Le catalogue s'appuie sur une classe CRUD générique typée (\texttt{CrudBase}) réutilisée par tous les routers, ce qui factorise les opérations \texttt{list / get / create / update / delete}. Le \texttt{catalog\_router} gère les référentiels (types d'objet, états d'usure, catégories), le \texttt{book\_router} expose les livres (notamment \texttt{GET /books/by-isbn/\{isbn\}} et \texttt{GET /books/\{id\}}) via un \texttt{BookService} dédié, et le \texttt{stock\_router} pilote les quantités disponibles et réservées. Les lectures du catalogue sont publiques ; les opérations d'écriture exigent un utilisateur authentifié (et un rôle \texttt{admin} pour la création de livres).

\subsection{Module scan ISBN}

Côté frontend, le code-barres est décodé directement dans le navigateur, puis l'ISBN est envoyé à l'API qui interroge \textbf{OpenLibrary} avec repli automatique sur \textbf{Google Books} (\texttt{GET /isbn/\{isbn\}}, cf. annexe~\ref{lst:isbn}) ; un \texttt{404} déclenche la saisie manuelle. Les images de couverture distantes sont rapatriées via \texttt{image\_service.download\_image} et re-servies depuis \texttt{/static} afin de ne pas dépendre d'URL tierces. Le \texttt{scan\_router} assure la traçabilité : chaque scan est enregistré dans la table \texttt{scan\_isbn} (rattaché à l'utilisateur courant et au livre concerné), avec vérification préalable de l'existence du livre.

\subsection{Module commande et paiement PostFinance}

Le \texttt{order\_router} gère le cycle de vie des commandes (\texttt{commande}, \texttt{ligne\_commande}, \texttt{paiement}). À la validation, une transaction PostFinance est créée (\texttt{create\_postfinance\_iframe\_session}) à partir des lignes et de l'adresse de facturation, et l'utilisateur est redirigé vers le tunnel de paiement. Le retour est traité par \textbf{webhook} : la signature est vérifiée (\texttt{verify\_postfinance\_webhook\_signature}) avant toute mise à jour, puis le statut de paiement est interprété (\texttt{is\_postfinance\_success\_status}). En cas de succès, la commande passe au statut payé et la fonction \texttt{\_finalize\_commande} transforme le stock réservé en stock vendu (avec verrou \texttt{with\_for\_update} pour éviter les accès concurrents). Un prestataire alternatif (Payrexx) avait été initialement intégré puis abandonné au profit de PostFinance.

\subsection{Back-office bénévole}

Le back-office réutilise les mêmes endpoints sécurisés pour offrir aux bénévoles une interface de gestion des annonces (création via scan ISBN ou saisie manuelle, modification, suppression) et de suivi du stock, sans exposer la complexité technique sous-jacente.

\section{Sécurité et architecture logicielle}

\subsection{Authentification JWT et gestion des rôles}

L'authentification repose sur le standard \textbf{JSON Web Token (JWT)}, implémenté côté backend avec la librairie \texttt{python-jose}. Le flux est le suivant :

\begin{enumerate}
    \item L'utilisateur soumet son email et son mot de passe via \texttt{POST /auth/token}.
    \item FastAPI récupère l'utilisateur en base via SQLAlchemy et vérifie le mot de passe avec \texttt{passlib} (bcrypt).
    \item Si les identifiants sont valides, un JWT signé est généré, contenant l'identifiant utilisateur, son rôle et une date d'expiration.
    \item Le token est retourné au client, qui le stocke et l'inclut dans l'en-tête HTTP \texttt{Authorization: Bearer <token>} de chaque requête protégée.
    \item Un middleware FastAPI (dépendance \texttt{get\_current\_user}) intercepte chaque requête, vérifie la signature JWT et extrait les informations de l'utilisateur.
\end{enumerate}

\noindent Les rôles définis dans le système sont :
\begin{itemize}
    \item \texttt{admin} : accès complet, CRUD sur toutes les entités, modération.
    \item \texttt{user} : consultation, favoris, passage de commande.
\end{itemize}

\subsection{Hashage des mots de passe avec bcrypt}

Le hashage des mots de passe est réalisé avec \textbf{bcrypt}, via la librairie Python \texttt{passlib}. Bcrypt est considéré comme l'algorithme de référence pour le hashage de mots de passe pour plusieurs raisons :

\begin{itemize}
    \item \textbf{Fonction de coût adaptative (\textit{cost factor}) :} Bcrypt intègre un paramètre de coût (appelé \textit{work factor} ou \textit{rounds}) qui permet d'augmenter le temps de calcul du hash au fur et à mesure que la puissance de calcul des machines augmente. Cela rend les attaques par force brute progressivement plus coûteuses.
    \item \textbf{Sel aléatoire intégré :} Bcrypt génère automatiquement un sel (\textit{salt}) unique et aléatoire pour chaque mot de passe haché. Deux utilisateurs ayant le même mot de passe auront des hashs différents, ce qui rend les attaques par table arc-en-ciel (\textit{rainbow table}) inopérantes.
    \item \textbf{Résistance aux GPU :} Contrairement à MD5 ou SHA-256, bcrypt est conçu pour être difficile à paralléliser sur GPU, ce qui ralentit considérablement les attaques par dictionnaire.
    \item \textbf{Standard éprouvé :} Bcrypt existe depuis 1999 et est recommandé par l'OWASP (\textit{Open Web Application Security Project}) comme algorithme de hashage de mots de passe.
\end{itemize}

\noindent La valeur du \textit{work factor} utilisée dans CARIBOOKS est de \textbf{12 rounds} (valeur par défaut de passlib), offrant un bon équilibre entre sécurité et performance.

\subsection{Protection contre les injections SQL}

L'utilisation de l'\textbf{ORM SQLAlchemy} protège nativement l'application contre les injections SQL. SQLAlchemy génère des requêtes paramétrées, dans lesquelles les valeurs utilisateur ne sont jamais interpolées directement dans la chaîne SQL, mais transmises comme paramètres liés (\textit{bound parameters}) au pilote de base de données. Même si un utilisateur malveillant tente d'injecter du SQL dans un champ de formulaire, la valeur sera traitée comme une donnée littérale et non comme une instruction SQL.

\noindent En complément, la validation des données d'entrée est systématiquement assurée par \textbf{Pydantic} (schémas de validation FastAPI) avant toute interaction avec la base de données.

\section{Difficultés rencontrées}

\begin{itemize}
    \item \textbf{Changement de technologie backend :} Une première version du backend avait été initiée avec Express.js (Node.js). Face à un niveau de maîtrise insuffisant de l'écosystème JavaScript côté serveur et aux exigences de robustesse du projet, le choix a été fait de migrer vers FastAPI (Python). Cette décision, bien que coûteuse en temps initialement, a permis de bénéficier d'une validation automatique des données, d'une documentation Swagger native et d'une meilleure productivité globale.
    \item \textbf{Performance du scan de code-barres :} La première implémentation du module de scan ISBN présentait une latence élevée et un taux de reconnaissance insuffisant. Des optimisations de l'algorithme de détection ont été apportées pour améliorer la précision et la rapidité de lecture.
    \item \textbf{Changement de prestataire de paiement :} Le prestataire initialement envisagé (Payrexx) a été remplacé par \textbf{PostFinance}, dont l'environnement de test sandbox ne nécessite pas de carte bancaire réelle avant la mise en production, ce qui a considérablement facilité le développement et les tests du tunnel de paiement.
    \item \textbf{Gestion du périmètre fonctionnel :} La demandeuse, dans son rôle de Product Owner informelle, souhaitait régulièrement ajouter de nouvelles fonctionnalités. Appliquer une gestion stricte du périmètre MVP, en expliquant les contraintes de temps et en reportant les demandes aux phases suivantes, a été un apprentissage professionnel important.
    \item \textbf{Conciliation apprentissage / projet personnel :} Le développement réalisé uniquement en soirée (17h–20h, 3 jours par semaine) a imposé une discipline de planification rigoureuse pour maintenir la progression du projet tout en respectant les engagements de la formation en alternance.
\end{itemize}

% --- CHAPITRE 4 ---
\chapter{Tests et validation}

\section{Stratégie de test}

Une stratégie de test à trois niveaux a été mise en place :

\begin{itemize}
    \item \textbf{Tests unitaires :} Vérification du comportement individuel de chaque fonction et service (ex. : validation des données ISBN, calcul du montant en CHF, hashage et vérification des mots de passe, génération et décodage des JWT). Réalisés avec \textbf{pytest}.
    \item \textbf{Tests fonctionnels :} Validation des parcours utilisateur complets (ajout d'un livre, processus d'achat, authentification). Réalisés avec \textbf{pytest} + \texttt{TestClient} FastAPI.
    \item \textbf{Tests d'intégration :} Vérification des interactions entre les modules (API, base de données MySQL via SQLAlchemy, API PostFinance sandbox, API ISBN). Réalisés avec \textbf{pytest}.
\end{itemize}

\section{Outils de test}

\begin{itemize}
    \item \textbf{pytest} : Framework de test Python, utilisé pour les tests unitaires, fonctionnels et d'intégration du backend FastAPI.
    \item \textbf{TestClient FastAPI} : Client HTTP de test fourni par FastAPI (basé sur \texttt{httpx}), permettant de tester les endpoints sans démarrer un vrai serveur.
    \item \textbf{Thunder Client} (extension VS Code) : Outil de test d'API REST similaire à Postman, utilisé pour tester manuellement les endpoints pendant le développement.
    \item \textbf{Swagger UI} : Documentation et test interactif des endpoints générés automatiquement par FastAPI, permettant de valider rapidement les contrats d'API.
    \item \textbf{Sandbox PostFinance} : Environnement de test fourni par PostFinance permettant de simuler des paiements réels sans carte bancaire réelle, avec des numéros de carte de test et des scénarios de succès/échec.
\end{itemize}

\section{Plan de tests — Principaux cas de test}

\begin{longtable}{@{}p{2cm}p{4cm}p{4cm}p{3.5cm}@{}}
\toprule
\textbf{Module} & \textbf{Cas de test} & \textbf{Données d'entrée} & \textbf{Résultat attendu} \\
\midrule
\endhead
Auth & Inscription valide & Email unique, mot de passe valide & 201, token JWT retourné \\
Auth & Inscription email existant & Email déjà en base & 400, message d'erreur \\
Auth & Connexion valide & Email + mot de passe corrects & 200, token JWT \\
Auth & Connexion mot de passe incorrect & Email valide, mauvais MDP & 401 \\
Auth & Route protégée sans token & Requête sans header Bearer & 403 \\
Auth & Route protégée token expiré & Token JWT expiré & 401 \\
ISBN & Scan ISBN reconnu & ISBN valide (ex. 9782070413119) & 200, métadonnées retournées \\
ISBN & Scan ISBN inconnu & ISBN non référencé & 404, fallback saisie manuelle \\
Articles & Création article & Données valides, token utilisateur & 201, article créé \\
Articles & Création sans auth & Données valides, sans token & 403 \\
Articles & Récupération catalogue & — & 200, liste paginée \\
Articles & Filtre par catégorie & id\_categorie valide & 200, liste filtrée \\
Commande & Passer commande & Panier non vide, user connecté & 201, commande créée \\
Paiement & Initiation paiement & Commande valide, sandbox PostFinance & Redirection URL PostFinance \\
Paiement & Webhook succès & Callback PostFinance success & Statut commande = PAYÉE \\
Paiement & Webhook échec & Callback PostFinance fail & Statut commande = ÉCHEC \\
\bottomrule
\caption{Plan de tests — Cas de test principaux}
\label{tab:tests}
\end{longtable}

\section{Résultats obtenus}

La suite de tests automatisés a été exécutée avec \textbf{pytest} et l'extension \texttt{pytest-cov}. L'intégralité des tests passe : \textbf{20 tests sur 20 réussis, soit 100\,\% de réussite}, répartis entre tests unitaires, tests d'intégration (via \texttt{TestClient}) et un test fonctionnel de bout en bout.

\noindent La \textbf{couverture de code globale atteint 70\,\%} (2\,326 instructions analysées, 708 non couvertes). Le tableau ci-dessous synthétise la couverture des principaux modules :

\vspace{5pt}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{@{}p{7cm}p{2.5cm}p{2.5cm}@{}}
\toprule
\textbf{Module} & \textbf{Instructions} & \textbf{Couverture} \\
\midrule
\texttt{infrastructure/models.py}       & 135 & 100\,\% \\
\texttt{presentation/schemas.py}        & 197 & 96\,\% \\
\texttt{presentation/scan\_router.py}    & 45  & 91\,\% \\
\texttt{presentation/catalog\_router.py} & 83  & 89\,\% \\
\texttt{presentation/auth\_router.py}    & 36  & 83\,\% \\
\texttt{services/jwt\_service.py}        & 14  & 86\,\% \\
\texttt{services/book\_service.py}       & 39  & 77\,\% \\
\texttt{presentation/book\_router.py}    & 139 & 68\,\% \\
\texttt{services/postfinance\_service.py}& 234 & 64\,\% \\
\texttt{services/image\_service.py}      & 56  & 23\,\% \\
\texttt{presentation/order\_router.py}   & 412 & 17\,\% \\
\midrule
\textbf{TOTAL} & \textbf{2\,326} & \textbf{70\,\%} \\
\bottomrule
\end{tabular}
\caption{Couverture de code par module (\texttt{pytest --cov})}
\label{tab:couverture}
\end{table}

\noindent Les modèles de données, les schémas de validation et les modules d'authentification et de catalogue sont très bien couverts (83\,\% à 100\,\%). Les modules les moins couverts sont le routeur de commande et de paiement (\texttt{order\_router}, 17\,\%) et le service de traitement d'images (23\,\%) : 
\vspace{5px}
ce sont précisément les parties identifiées comme prioritaires pour la consolidation (voir la section \og Limites et bugs restants \fg{}). Aucun test de charge ni de performance n'a été réalisé dans le cadre de ce MVP.

\includegraphics[width=\textwidth]{PYTEST_RESULT_SCREENSHOT.png}

\section{Analyse des performances}

Aucune campagne formelle de tests de performance ou de charge (\textit{benchmark}, montée en charge, \textit{stress test}) n'a été menée durant cette première itération : la priorité du MVP portait sur la couverture fonctionnelle et la sécurité. L'architecture retenue offre néanmoins de bonnes bases de performance : FastAPI repose sur un serveur ASGI asynchrone (Uvicorn) reconnu pour ses temps de réponse faibles, l'ORM SQLAlchemy s'appuie sur des index dédiés (voir le script DDL en annexe~\ref{lst:ddl}) pour accélérer les requêtes fréquentes du catalogue, et la conteneurisation Docker garantit la reproductibilité des conditions d'exécution. La mise en place de tests de charge (par exemple avec \texttt{locust} ou \texttt{k6}) et d'un audit Lighthouse du frontend constitue un axe d'amélioration identifié pour la phase suivante.

\section{Limites et bugs restants}

À l'issue de cette première itération, plusieurs limites et points de consolidation ont été identifiés :

\begin{itemize}
    \item \textbf{Fiabilisation du paiement :} la fonctionnalité de paiement (intégration PostFinance, gestion des webhooks et mise à jour des statuts de commande) reste à corriger et à fiabiliser. C'est le module le moins couvert par les tests automatisés (\texttt{order\_router} à 17\,\%).
    \item \textbf{Fluidité du tunnel d'achat :} le flux de paiement doit être amélioré afin de rendre le passage à l'achat puis au paiement plus simple et plus intuitif pour l'acheteur.
    \item \textbf{Interface mobile :} certains écrans de l'interface mobile restent à affiner pour une expérience pleinement responsive.
    \item \textbf{Suivi des commandes côté administrateur :} le suivi et la gestion des commandes par l'administrateur doivent être complétés et améliorés.
    \item \textbf{Couverture de tests :} la couverture des modules de commande/paiement et de traitement d'images est à renforcer.
\end{itemize}

% ==========================================
% CHAPITRE G — CYBERSÉCURITÉ ET QUALITÉ
% ==========================================
\chapter{Cybersécurité et qualité logicielle}

\section{Synthèse des mesures de sécurité}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{@{}p{5cm}p{4cm}p{5cm}@{}}
\toprule
\textbf{Menace / Risque} & \textbf{Mesure mise en place} & \textbf{Technologie utilisée} \\
\midrule
Injections SQL & Requêtes paramétrées & SQLAlchemy ORM \\
Mots de passe compromis & Hashage fort & bcrypt via passlib \\
Accès non autorisé aux routes & Authentification obligatoire & JWT + middleware FastAPI \\
Élévation de privilèges & Contrôle des rôles & Rôles admin / seller / user \\
Données invalides côté serveur & Validation stricte & Pydantic (schémas FastAPI) \\
Tokens interceptés & Expiration courte + HTTPS & JWT \texttt{exp}, TLS en prod \\
Informations sensibles en clair & Variables d'environnement & Fichiers \texttt{.env}, \texttt{python-dotenv} \\
\bottomrule
\end{tabular}
\caption{Mesures de cybersécurité implémentées dans CARIBOOKS}
\label{tab:securite}
\end{table}

\section{Gestion sécurisée des sessions et tokens JWT}

L'accès aux ressources protégées de l'API CARIBOOKS repose exclusivement sur des \textbf{JSON Web Tokens (JWT)}. Chaque token contient :
\begin{itemize}
    \item \texttt{sub} : identifiant de l'utilisateur
    \item \texttt{role} : rôle (admin / user)
    \item \texttt{exp} : timestamp d'expiration
\end{itemize}

\noindent Les tokens sont signés avec un algorithme \textbf{HS256} (HMAC-SHA256) et une clé secrète stockée dans les variables d'environnement (jamais en clair dans le code source). La durée de validité est configurée pour limiter la fenêtre d'exposition en cas d'interception.

\section{Gestion des droits et des accès}

Chaque endpoint FastAPI déclare explicitement ses dépendances d'authentification et d'autorisation. Les routes de lecture publique (catalogue, fiche livre) sont accessibles sans token. Les routes d'écriture (création d'annonce, passage de commande) requièrent un token valide. Les routes d'administration requièrent un rôle \texttt{admin} vérifié dans le payload JWT.

\section{Veille technologique et sécurité}

Dans le cadre du développement de CARIBOOKS, une veille régulière a été menée sur les ressources suivantes :
\begin{itemize}
    \item \textbf{OWASP Top 10} : référentiel des dix vulnérabilités web les plus critiques, consulté pour orienter les choix de sécurisation (injections SQL, broken authentication, security misconfiguration).
    \item \textbf{Documentation officielle FastAPI} (sécurité) : bonnes pratiques pour OAuth2, JWT, hashage.
    \item \textbf{Fireship} (YouTube) : veille technique sur les tendances web et les pratiques de développement modernes.
    \item \textbf{GitHub Security Advisories} : alertes automatiques sur les dépendances Python du projet via Dependabot.
    \item \textbf{Daily.DEV et bytes} : 
    Newsletter email sur les dernière nouveautées informatique
\end{itemize}

% ==========================================
% CHAPITRE H — COMMUNICATION ET COLLABORATION
% ==========================================
\chapter{Communication et collaboration}

\section{Relations avec les acteurs du projet}

\subsection{La Product Owner — bénévole Caritas}

La demandeuse du projet est une bénévole de la recyclerie Caritas, avec peu de connaissances techniques. La communication a donc été adaptée en conséquence :

\begin{itemize}
    \item \textbf{Réunions informelles régulières :} les échanges se tenaient de manière informelle, en présentiel ou par message, à chaque fin de sprint. La demandeuse formulait ses retours après avoir vu une démonstration de l'avancement.
    \item \textbf{Démos visuelles systématiques :} chaque nouvelle fonctionnalité était présentée sous forme de démonstration en direct (ou via capture vidéo) plutôt qu'expliquée techniquement, afin de permettre à la Product Owner de se prononcer sur l'adéquation au besoin.
    \item \textbf{Reformulation des besoins :} les demandes fonctionnelles exprimées en langage courant étaient reformulées en user stories et tickets Planner, puis confirmées auprès de la demandeuse avant d'être planifiées.
    \item \textbf{Gestion des demandes hors périmètre :} plusieurs fonctionnalités ont été demandées en cours de projet (messagerie, avis, estimation de prix). Ces demandes ont été enregistrées dans un backlog de phase 2/3 et expliquées à la demandeuse avec les contraintes de temps associées.
\end{itemize}

\subsection{Encadrant pédagogique}

Les échanges avec l'encadrant pédagogique (M. Mhand Boufala, ESTIAM) se sont tenus dans le cadre des suivis de formation, avec présentation de l'avancement du projet et validation des choix techniques retenus.

\section{Travail en solo et organisation personnelle}

Le projet CARIBOOKS a été développé en totalité par une seule personne, sans équipe technique. Cette contrainte a imposé une organisation rigoureuse :

\begin{itemize}
    \item Utilisation de \textbf{Microsoft Planner} pour maintenir un backlog structuré et suivre la progression sprint par sprint.
    \item Découpage des tâches en unités de travail compatibles avec des séances de développement de 3 heures (17h–20h).
    \item Priorisation systématique des fonctionnalités selon la valeur utilisateur (MVP first).
    \item Recours à la documentation officielle et à la veille technique pour pallier l'absence d'un pair reviewer.
\end{itemize}

% ==========================================
% 7. CONCLUSION GÉNÉRALE
% ==========================================
\chapter*{Conclusion générale}
\addcontentsline{toc}{chapter}{Conclusion générale}

\section*{Apports du projet}

Ce projet a permis de concevoir et de déployer une application e-commerce full-stack répondant à un besoin utilisateur réel. Sur le plan technique, il a approfondi la maîtrise du déploiement d'applications conteneurisées (Docker, CI/CD GitHub Actions), de l'intégration d'un système de paiement suisse (PostFinance) et du développement d'une API REST avec FastAPI. Il représente également une progression significative par rapport à une première tentative de projet plus simple, avec une architecture plus robuste et une démarche de développement plus rigoureuse.

\noindent Sur le plan professionnel, ce projet a été l'occasion d'apprendre à gérer les attentes d'un client non technique (refus de certaines fonctionnalités hors périmètre, conduite de démos adaptées), à travailler en méthodologie Agile en autonomie complète, et à prendre des décisions architecturales motivées (abandon d'Express.js au profit de FastAPI, choix de PostFinance plutôt que Payrexx, adoption de SQLAlchemy comme ORM).

\noindent Ce projet illustre également la capacité à mener un développement solo dans des contraintes de temps réelles — le soir, en parallèle d'une formation en apprentissage — tout en maintenant un niveau de qualité technique conforme aux exigences du référentiel CDA.

\section*{Limites}

Malgré l'atteinte des objectifs du MVP, plusieurs limites subsistent à la fin de cette première itération :

\begin{itemize}
    \item \textbf{Paiement à fiabiliser :} le tunnel de paiement PostFinance (webhooks, mise à jour des statuts) doit encore être corrigé et consolidé ; c'est le module le moins couvert par les tests (17\,\%).
    \item \textbf{Parcours d'achat :} le passage à l'achat et au paiement reste à fluidifier pour l'acheteur.
    \item \textbf{Suivi des commandes :} la gestion et le suivi des commandes côté administrateur sont à compléter.
    \item \textbf{Interface mobile :} certains écrans restent à affiner pour une expérience pleinement responsive.
    \item \textbf{Fonctionnalités hors MVP :} la messagerie acheteur-vendeur, les avis et l'estimation automatique de prix n'ont pas été implémentés (renvoyés aux phases 2 et 3).
    \item \textbf{Couverture de tests :} à renforcer sur les modules de commande/paiement et de traitement d'images.
\end{itemize}

\section*{Perspectives d'évolution}

Plusieurs axes d'évolution sont envisagés pour les phases suivantes du projet :
\begin{itemize}
    \item \textbf{Phase 2 :} Ajout d'un système de messagerie entre acheteur et vendeur pour permettre la négociation du prix, ainsi que d'un système d'avis et de commentaires sur les livres pour renforcer la confiance entre utilisateurs.
    \item \textbf{Phase 3 :} Estimation automatique du prix de revente basée sur les données du marché, recommandations personnalisées basées sur l'historique de navigation, gestion avancée des livraisons avec génération d'étiquettes PostPac intégrées via l'API de la Poste Suisse.
    \item \textbf{Évolutions techniques :} Migration possible vers une base de données PostgreSQL pour une meilleure gestion des performances à grande échelle, mise en place d'un système de cache (Redis) pour les requêtes fréquentes sur le catalogue, et intégration d'un CDN pour les images de couverture des livres.
\end{itemize}

% ==========================================
% 8. BIBLIOGRAPHIE
% ==========================================
\chapter*{Bibliographie}
\addcontentsline{toc}{chapter}{Bibliographie}

\begin{itemize}
    \item \textbf{Documentation officielle FastAPI} — \url{https://fastapi.tiangolo.com/}
    \item \textbf{Documentation officielle FastAPI — Sécurité / OAuth2 + JWT} — \url{https://fastapi.tiangolo.com/tutorial/security/oauth2-jwt/}
    \item \textbf{Documentation officielle Next.js} — \url{https://nextjs.org/docs}
    \item \textbf{Documentation officielle SQLAlchemy} — \url{https://docs.sqlalchemy.org/}
    \item \textbf{Documentation PostFinance (API Paiement)} — \url{https://docs.postfinance.ch/}
    \item \textbf{Open Library API} — \url{https://openlibrary.org/developers/api}
    \item \textbf{passlib — bcrypt documentation} — \url{https://passlib.readthedocs.io/}
    \item \textbf{OWASP Top 10} — \textit{Les dix vulnérabilités web les plus critiques} — \url{https://owasp.org/www-project-top-ten/}
    \item \textbf{Fireship} (veille technique, tutoriels vidéo) — \url{https://www.youtube.com/@Fireship}
    \item \textbf{Ministère de la Culture / SOFIA} — \textit{Étude sur le marché du livre d'occasion}, 2024 — \url{https://www.la-sofia.org/action-interprofessionnelle/etude-sur-le-livre-doccasion/}
    \item \textbf{DPD} — \textit{Sondage sur la seconde main en Europe}, 2024 — \url{https://www.20min.ch/fr/story/economie-circulaire-les-suisses-champions-d-europe-de-la-seconde-main-103192492}
    \item \textbf{pytest documentation} — \url{https://docs.pytest.org/}
    \item \textbf{Docker documentation} — \url{https://docs.docker.com/}
    \item \textit{[Ajouter les IA utilisées (ChatGPT, Copilot, Perplexity) et toute autre source consultée]}
\end{itemize}

% ==========================================
% 9. ANNEXES
% ==========================================
\chapter*{Annexes}
\addcontentsline{toc}{chapter}{Annexes}

\section*{Annexe A — Liens du projet}

Le code source complet du projet est disponible sur GitHub :
\begin{center}
    \url{https://github.com/Visualstudiocodetest/plobooks}
\end{center}

\noindent L'application est déployée et accessible en ligne à l'adresse suivante :
\begin{center}
    \url{https://caribooks.vercel.app}
\end{center}

\noindent L'hébergement repose sur une architecture hybride : le \textbf{frontend Next.js} est déployé sur \textbf{Vercel}, tandis que le \textbf{backend FastAPI} et la \textbf{base de données MySQL} sont hébergés sur \textbf{Oracle Cloud} (machine virtuelle et instance MySQL).

\section*{Annexe B — Script SQL de création de la base de données}

Le script DDL ci-dessous présente la création complète de la base de données MySQL de CARIBOOKS.

\begin{lstlisting}[style=sqlstyle, caption={Script DDL — Création de la base de données CARIBOOKS}, label={lst:ddl}]
CREATE DATABASE caribooks;
USE caribooks;

CREATE TABLE type_objet (
    id_type_objet BIGINT AUTO_INCREMENT PRIMARY KEY,
    libelle VARCHAR(100) NOT NULL,
    code VARCHAR(50) NOT NULL UNIQUE,
    description TEXT
);

CREATE TABLE etat_usure (
    id_etat_usure BIGINT AUTO_INCREMENT PRIMARY KEY,
    libelle VARCHAR(100) NOT NULL UNIQUE,
    description TEXT
);

CREATE TABLE categorie (
    id_categorie BIGINT AUTO_INCREMENT PRIMARY KEY,
    libelle VARCHAR(100) NOT NULL UNIQUE,
    description TEXT
);

CREATE TABLE article (
    id_article BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_type_objet BIGINT NOT NULL,
    id_etat_usure BIGINT NOT NULL,
    sku VARCHAR(100) NOT NULL UNIQUE,
    titre VARCHAR(255) NOT NULL,
    description TEXT,
    prix_chf DECIMAL(10,2) NOT NULL CHECK (prix_chf >= 0),
    actif BOOLEAN NOT NULL DEFAULT TRUE,
    date_creation TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_type_objet)
        REFERENCES type_objet(id_type_objet)
        ON DELETE RESTRICT ON UPDATE CASCADE,
    FOREIGN KEY (id_etat_usure)
        REFERENCES etat_usure(id_etat_usure)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE livre (
    id_article BIGINT PRIMARY KEY,
    isbn VARCHAR(20) NOT NULL UNIQUE,
    auteur VARCHAR(255),
    editeur VARCHAR(255),
    date_publication DATE,
    langue VARCHAR(50),
    FOREIGN KEY (id_article)
        REFERENCES article(id_article)
        ON DELETE CASCADE ON UPDATE CASCADE
);

CREATE TABLE article_categorie (
    id_article BIGINT NOT NULL,
    id_categorie BIGINT NOT NULL,
    PRIMARY KEY (id_article, id_categorie),
    FOREIGN KEY (id_article)
        REFERENCES article(id_article)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (id_categorie)
        REFERENCES categorie(id_categorie)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE source_stock (
    id_source_stock BIGINT AUTO_INCREMENT PRIMARY KEY,
    libelle VARCHAR(150) NOT NULL,
    type_source VARCHAR(50) NOT NULL,
    description TEXT
);

CREATE TABLE stock (
    id_stock BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_article BIGINT NOT NULL,
    id_source_stock BIGINT NOT NULL,
    quantite_disponible INT NOT NULL DEFAULT 0
        CHECK (quantite_disponible >= 0),
    quantite_reservee INT NOT NULL DEFAULT 0
        CHECK (quantite_reservee >= 0),
    date_mise_a_jour TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_stock_article_source
        UNIQUE (id_article, id_source_stock),
    FOREIGN KEY (id_article)
        REFERENCES article(id_article)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (id_source_stock)
        REFERENCES source_stock(id_source_stock)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE utilisateur (
    id_utilisateur BIGINT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(100) NOT NULL,
    prenom VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    mot_de_passe_hash VARCHAR(255) NOT NULL,
    role VARCHAR(50) NOT NULL DEFAULT 'user',
    date_creation TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    billing_address_line1 VARCHAR(255) NULL,
    billing_address_line2 VARCHAR(255) NULL,
    billing_postal_code VARCHAR(30) NULL,
    billing_city VARCHAR(100) NULL,
    billing_country VARCHAR(100) NULL,
    billing_phone VARCHAR(50) NULL
);

CREATE TABLE commande (
    id_commande BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_utilisateur BIGINT NOT NULL,
    numero_commande VARCHAR(50) NOT NULL UNIQUE,
    date_commande TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    montant_total_chf DECIMAL(10,2) NOT NULL
        CHECK (montant_total_chf >= 0),
    statut VARCHAR(50) NOT NULL,
    shipping_method VARCHAR(50) NOT NULL DEFAULT 'POST',
    frais_port_chf DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    FOREIGN KEY (id_utilisateur)
        REFERENCES utilisateur(id_utilisateur)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE ligne_commande (
    id_ligne_commande BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_commande BIGINT NOT NULL,
    id_article BIGINT NOT NULL,
    quantite INT NOT NULL CHECK (quantite > 0),
    prix_unitaire_chf DECIMAL(10,2) NOT NULL
        CHECK (prix_unitaire_chf >= 0),
    FOREIGN KEY (id_commande)
        REFERENCES commande(id_commande)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (id_article)
        REFERENCES article(id_article)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE paiement (
    id_paiement BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_commande BIGINT NOT NULL,
    fournisseur_paiement VARCHAR(50) NOT NULL DEFAULT 'POSTFINANCE',
    reference_externe VARCHAR(255) NOT NULL UNIQUE,
    montant_chf DECIMAL(10,2) NOT NULL CHECK (montant_chf >= 0),
    devise CHAR(3) NOT NULL DEFAULT 'CHF',
    statut VARCHAR(50) NOT NULL,
    date_paiement TIMESTAMP NULL DEFAULT NULL,
    FOREIGN KEY (id_commande)
        REFERENCES commande(id_commande)
        ON DELETE RESTRICT ON UPDATE CASCADE
);

CREATE TABLE scan_isbn (
    id_scan_isbn BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_utilisateur BIGINT NOT NULL,
    id_article_livre BIGINT NOT NULL,
    isbn_lu VARCHAR(20) NOT NULL,
    date_scan TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    valide BOOLEAN NOT NULL DEFAULT FALSE,
    FOREIGN KEY (id_utilisateur)
        REFERENCES utilisateur(id_utilisateur)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (id_article_livre)
        REFERENCES livre(id_article)
        ON DELETE CASCADE ON UPDATE CASCADE
);

-- Index de performance
CREATE INDEX idx_article_type_objet ON article(id_type_objet);
CREATE INDEX idx_article_etat_usure ON article(id_etat_usure);
CREATE INDEX idx_stock_article ON stock(id_article);
CREATE INDEX idx_stock_source ON stock(id_source_stock);
CREATE INDEX idx_commande_utilisateur ON commande(id_utilisateur);
CREATE INDEX idx_ligne_commande_commande ON ligne_commande(id_commande);
CREATE INDEX idx_ligne_commande_article ON ligne_commande(id_article);
CREATE INDEX idx_paiement_commande ON paiement(id_commande);
CREATE INDEX idx_scan_utilisateur ON scan_isbn(id_utilisateur);
CREATE INDEX idx_scan_livre ON scan_isbn(id_article_livre);
CREATE INDEX idx_livre_isbn ON livre(isbn);
\end{lstlisting}

\section*{Annexe C — Extraits de code significatifs}

\subsection*{C.1 — Génération du JWT et endpoint de connexion (FastAPI)}

\begin{lstlisting}[style=pythonstyle, caption={Service JWT (HS256) et endpoint POST /auth/token}, label={lst:auth}]
# services/jwt_service.py
from datetime import datetime, timedelta
from jose import jwt

def create_access_token(payload: dict, secret_key: str,
                        expires_minutes: int) -> str:
    to_encode = payload.copy()
    expire = datetime.utcnow() + timedelta(minutes=expires_minutes)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, secret_key, algorithm="HS256")

# presentation/auth_router.py
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from infrastructure import crud_user
from presentation.deps import get_db
from presentation.auth_schemas import LoginRequest, Token
from services.jwt_service import create_access_token

ACCESS_TOKEN_EXPIRE_MINUTES = 480
router = APIRouter(prefix="/auth", tags=["auth"])

@router.post("/token", response_model=Token)
def login_for_access_token(payload: LoginRequest,
                           db: Session = Depends(get_db)):
    user = crud_user.get_user_by_email(db, str(payload.username))
    verified = bool(user) and crud_user.verify_password(
        str(payload.password), str(user.mot_de_passe_hash)
    )
    if not user or not verified:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Incorrect credentials",
        )
    to_encode = {"sub": user.email, "role": user.role}
    access_token = create_access_token(
        to_encode, SECRET_KEY, ACCESS_TOKEN_EXPIRE_MINUTES
    )
    return Token(access_token=access_token)
\end{lstlisting}

\subsection*{C.2 — Endpoint scan ISBN (lookup API externe)}

\begin{lstlisting}[style=pythonstyle, caption={Endpoint GET /isbn/\{isbn\} — Lookup Open Library}, label={lst:isbn}]
import httpx
from fastapi import APIRouter, HTTPException

router = APIRouter(prefix="/isbn", tags=["isbn"])

OPENLIBRARY_URL = "https://openlibrary.org/api/books"
GOOGLE_BOOKS_URL = "https://www.googleapis.com/books/v1/volumes"

@router.get("/{isbn}")
async def lookup_isbn(isbn: str):
    # Tentative Open Library
    async with httpx.AsyncClient() as client:
        resp = await client.get(
            OPENLIBRARY_URL,
            params={"bibkeys": f"ISBN:{isbn}", "format": "json",
                    "jscmd": "data"}
        )
        data = resp.json()
        key = f"ISBN:{isbn}"
        if key in data:
            book = data[key]
            return {
                "isbn": isbn,
                "titre": book.get("title"),
                "auteur": book.get("authors", [{}])[0].get("name"),
                "editeur": book.get("publishers", [{}])[0].get("name"),
                "couverture": book.get("cover", {}).get("medium"),
                "source": "openlibrary"
            }

        # Fallback Google Books
        resp2 = await client.get(
            GOOGLE_BOOKS_URL,
            params={"q": f"isbn:{isbn}"}
        )
        gdata = resp2.json()
        if gdata.get("totalItems", 0) > 0:
            info = gdata["items"][0]["volumeInfo"]
            return {
                "isbn": isbn,
                "titre": info.get("title"),
                "auteur": ", ".join(info.get("authors", [])),
                "editeur": info.get("publisher"),
                "couverture": info.get("imageLinks", {}).get("thumbnail"),
                "source": "google_books"
            }

    raise HTTPException(
        status_code=404,
        detail="ISBN non trouvé. Veuillez saisir les informations manuellement."
    )
\end{lstlisting}




\section*{Annexe E — Documentation API Swagger}



\includegraphics[width=\textwidth]{Fast_API_SWAGGER_OVERVIEW.png}
\includegraphics[width=\textwidth]{SWAGGER_LOGIN_DETAIL.png}

\section*{Annexe F — Diagrammes UML détaillés}

L'ensemble des diagrammes UML et Merise du projet est présenté directement dans le chapitre \og Analyse et conception \fg{}. Pour rappel, le dossier comprend :

\begin{itemize}
    \item Diagramme de contexte (figure~\ref{fig:contexte}) ;
    \item Diagrammes de cas d'utilisation : scan ISBN (figure~\ref{fig:use_case_isbn}), paiement (figure~\ref{fig:use_case_payment}), vue globale (figure~\ref{fig:use_case_global}) ;
    \item Diagrammes d'activités : parcours d'achat (figure~\ref{fig:activites_achat}) et scan ISBN (figure~\ref{fig:activites_isbn}) ;
    \item Diagrammes de séquence : scan ISBN (figure~\ref{fig:sequence_isbn}), paiement (figure~\ref{fig:sequence_paiement}), authentification (figure~\ref{fig:sequence_auth}) ;
    \item Diagramme de classes (figure~\ref{fig:classes}) et diagramme d'objets (figure~\ref{fig:objets}) ;
    \item Diagramme de composants (figure~\ref{fig:composants}) et diagramme de déploiement (figure~\ref{fig:deploiement}) ;
    \item Modélisation Merise : MCD (figure~\ref{fig:mcd}) et MLD (figure~\ref{fig:mld}).
\end{itemize}

\end{document}