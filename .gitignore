.env
backend/.env
backend/venv/
backend/__pycache__
frontend/node_modules/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.log
.DS_Store
.vscode/
.next/
frontend/.next/
frontend/.pytest_cache/
# Track the Mermaid diagram SOURCES + render script (the CDA dossier references
# them and must be reproducible); only the generated PDFs are build artifacts.
diagrams/*.pdf

# Personal jury-prep notes (repo is public) — local only.
docs/
